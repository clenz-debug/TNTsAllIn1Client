package com.tntsallin1client.design;

/** Implemented on {@code TitleScreen} by {@code TitleScreenMixin} - lets {@code TitleScreenIntegration} tell which layout a title screen currently shows. */
public interface TitleScreenLayoutAccess {
	boolean tntsallin1client$isClientLayout();
}
