# Aktuelle Phase

**Phase 0 — Setup & externe Abhängigkeit anstoßen** ✅ abgeschlossen

**Phase 1 — "Hello World"-Fabric-Mod** ✅ abgeschlossen

**Phase 2 — Erstes Mixin** ✅ abgeschlossen

**Phase 3 — Electron-Launcher-MVP** 🔧 in Arbeit — alles außer dem echten Mojang-Login steht und ist vom Nutzer live bestätigt; wartet weiter auf die Mojang-API-Freischaltung (kein Blocker mehr fürs Weiterarbeiten, siehe Phase 4)

**Phase 4 — Fabric-Start** ✅ abgeschlossen

**Phase 5 — Eigene Features, leicht → schwer** 🔧 in Arbeit — 5a–5f, 5p und jetzt auch 5r abgeschlossen und live vom Nutzer bestätigt (5d ist bewusst nur ein erster Wurf, siehe unten); **5g–5o jetzt alle implementiert und gebaut** (kompletter zweiter Durchgang), warten noch auf Live-Bestätigung im Spiel; **5q (Blockumriss-Farbe) neu implementiert und gebaut**, noch nicht live getestet; **5r (Dunkles Inventar) nach vier Mixin-Bugfix-Runden komplett auf einen gebündelten Resourcepack umgestellt** (siehe 5r-Abschnitt unten) und live bestätigt ("deutlich besser")

## Phase 5 — Erledigt
Alle Unterpunkte 5a–5f umgesetzt, jeder einzeln vom Nutzer im laufenden Spiel bestätigt (`runClient`). Neue Packages in `mod/src/main/java/com/tntsallin1client/`: `hud/`, `inventory/`, `menu/`, `keybind/`, `debug/`, `config/`.

**5a — Koordinaten + Kompass (`hud/CoordinatesHud.java`):** Eigenes `HudElement` über `HudElementRegistry` (die aktuelle Fabric-API für HUD-Overlays in 1.21.11 — nicht das ältere `HudRenderCallback`). Zeigt Koordinaten + 8-Wege-Himmelsrichtung + Gradzahl, jedes der drei Teile einzeln über die Config schaltbar. Blendet sich bei offenem F3 aus (Konvention gilt seither für alle eigenen HUD-Texte, siehe Phase-2-Label unten).

**5b — Material-Zähler (`hud/MaterialCounterHud.java`):** Zählt ein Item über Hauptinventar + Offhand. Zwei Modi: festes Item per ID oder automatisch das aktuell gehaltene Item (`materialCounterUseHeldItem`) — umschaltbar im dedizierten Options-Screen des Features.

**5c — Inventar-Schnellsortierung (`inventory/InventorySorter.java`, `QuickSortUi.java`):** Sortiert Hauptinventar+Hotbar (Slots 9–44, Rüstung/Offhand bleiben unangetastet) in zwei Phasen (Stacks zusammenführen, dann alphabetisch nach Item-ID gruppieren) — jede Bewegung läuft über `MultiPlayerGameMode.handleInventoryMouseClick(...)`, also exakt denselben Klick-Mechanismus wie ein echter Mausklick, damit der Server synchron bleibt (kein direktes Herumschieben im Client-Array). Button sitzt außerhalb des Inventar-Panels rechts (vermeidet Überlappung mit dem "Crafting"-Label). Funktioniert sowohl im normalen Inventar (`InventoryScreen`) als auch im Creative-Modus (`CreativeModeInventoryScreen`, dort nur im "Inventory"-Tab — andere Tabs sind nur eine virtuelle Item-Auswahl, kein echtes Inventar). Tastenkombi standardmäßig unbelegt, änderbar über Minecrafts normales Steuerung-Menü (eigene Kategorie) — funktioniert bei offenem Screen nur über Fabrics `ScreenKeyboardEvents.afterKeyPress`, da normales `KeyMapping.consumeClick()` bei offenem Screen laut Vanilla-Quellcode (`KeyboardHandler`) gar nicht erst registriert wird.

**5d — F3-Verbesserungen (`debug/QuickInfoDebugEntry.java`, `debug/SystemInfoOverlay.java`, `menu/F3OptionsScreen.java`):** Nutzerfeedback war zunächst unspezifisch ("bessere Beschreibung was die Sachen machen, oder wichtigeres highlighten") — erste Version: ein zusätzlicher, farblich abgesetzter "Quick Info"-Block (Position, Biom, Lichtlevel inkl. Mob-Spawn-Hinweis, FPS) in Klartext-Labels statt vanilla-Kryptik. Technisch überraschend einfach: 1.21.11 hat das F3-Menü komplett auf ein modulares `DebugScreenEntry`-System umgebaut (`DebugScreenEntries.register(...)`, öffentliche API) — kein Mixin in Mojang-Code nötig, genau die Fragilität umgangen, vor der die Roadmap für diese Phase warnt.

Zweite Runde, konkreteres Feedback: (1) CPU/GPU/Java/Minecraft-Version raus aus dem Hauptbildschirm, stattdessen eigene Seite über **F3 halten + K** (`SystemInfoOverlay`, zentriert, wiederverwendet Mojangs eigene `DebugEntrySystemSpecs`/`DebugEntryVersion`-Objekte über einen simplen Zeilen-Sammler statt Hardware-Abfrage nachzubauen); (2) Biom-Namen ohne "minecraft:"-Präfix (nur bei Vanilla-Biomen, Mod-/Datapack-Biome behalten ihren Namespace zur Eindeutigkeit). Vanillas eigene Biom-/System-Specs-/Versions-Zeilen werden dabei über dieselbe `DebugScreenEntryStatus`-API ausgeblendet, reversibel über den bestehenden "F3 Quick Info"-Toggle.

**Stolperer unterwegs, gelöst:** Erste Wahl F3+**S** kollidierte mit Vanillas eigenem "Dump Dynamic Textures"-Hotkey — beim ersten Check per `grep` übersehen, weil die betreffende `KeyMapping`-Deklaration in `Options.java` mehrzeilig ist und nur die erste Zeile gematcht wurde. Auf F3+**K** gewechselt, diesmal jede `keyDebugXxx`-Vorbelegung einzeln gegengeprüft. Zusätzlich auf Nutzerwunsch: die Taste ist jetzt nicht nur über Vanillas Steuerung-Menü änderbar, sondern auch direkt im Mod-Menü (`F3OptionsScreen`, gleiches "Klicken, dann Taste drücken, Escape entbindet"-Verhalten wie Vanillas eigenes Rebind-UI).

**Bewusst nicht als final zu betrachten** — kann jederzeit weiter nachgeschärft werden, falls neues Feedback kommt.

**5p — Echte 3D-Blockmodelle:** Nutzer wollte ursprünglich das komplette "3D Texturen"-Idee-Item angehen; Rückfrage ergab: gemeint sind echte 3D-Blockmodelle (3D-Fackeln, -Ranken, -Truhen etc. mit zusätzlicher Geometrie statt nur flacher Textur), nicht die CTM-Pseudo-3D-Optik — letztere ist bereits mit 5f (Continuity) abgedeckt. Geprüft: das ist reine Vanilla-Resourcepack-Funktionalität, Sodium 0.8.13 rendert Custom-Block-Modelle mit zusätzlicher Geometrie nativ (kein Indium nötig — seit Sodium 0.6.0 eingebaut). Der einzige existierende Mixin (`GuiMixin`, HUD) berührt die Block-/Modell-Rendering-Pipeline nicht — keine Kollisionsgefahr. Ergebnis: **kein Mod-Code nötig.**

**Live vom Nutzer bestätigt:** Zip aus `launcher/resourcepacks-bundle/` in die reale Launcher-Instanz kopiert (`instanceDir()` aus `launcher/src/main/launch/installer.ts` → `%APPDATA%\tntsallin1client-launcher\instances\default\game\resourcepacks\`), über Optionen → Ressourcenpakete aktiviert, im laufenden Spiel getestet — platzierte Tür wird als echtes 3D-Modell gerendert statt flacher Textur. ✅

Auf Nutzerwunsch stattdessen ein passendes Resourcepack recherchiert und gebündelt: **[3D Default](https://modrinth.com/resourcepack/3d-default)** (GeForceLegend, aktiv gepflegt, explizit für 1.21.11 gelistet über Minecrafts eigenes Overlay-System in `pack.mcmeta` — die Datei wählt beim Laden automatisch den richtigen Versions-Unterordner, kein manuelles Zusammenführen nötig). Version `1.15.0` für `1.21.2+`, Datei per Modrinth-API-Version-Endpoint geladen und SHA-1 gegen den von Modrinth gemeldeten Hash verifiziert (`cc61b586aaae83497f77d79ceeffc4009950cd54`, 943.801 Bytes, exakt passend). Liegt in `launcher/resourcepacks-bundle/` (git-ignoriert, kein Source, gleiches Muster wie `launcher/mods-bundle/` aus Phase 4).

**Lizenz-Hinweis, wichtig:** GPL-3.0-only — bewusst **nicht** als Fabric-`registerBuiltinResourcePack` in den eigenen Mod-Jar eingebettet (das wäre technisch der 5f/Continuity-Weg gewesen), sondern als separate, unveränderte Datei neben dem Mod belassen, um in der GPLv3-§5-"mere aggregation"-Zone zu bleiben statt eine Ableitung/Kombination mit dem eigenen, anders lizenzierten Mod-Code zu riskieren (anders als Sodium/PolyForm und Lithium/Continuity/LGPL ist das die erste **volle** Copyleft-Lizenz im Projekt) — siehe Roadmap-Lizenzabschnitt, vor öffentlichem Release nochmal gegenprüfen.

**Für den Nutzer, um live zu testen:** Datei landet jetzt automatisch bei jedem Start in `<instance>/game/resourcepacks/` (siehe Bundle-Sync-Eintrag weiter unten) — im Spiel nur noch einmalig unter Optionen → Ressourcenpakete aktivieren (in die "Ausgewählt"-Liste ziehen), genau wie beim 5f-Default-CTM-Pack. Das Aktivieren selbst bleibt (wie bei jedem Vanilla-Resourcepack) ein einmaliger manueller Schritt pro frischer Instanz.

**Nachtrag (Bugreport, Folgesession):** Nutzer meldete fehlende 3D-Texturen in der über den eigenen Launcher gestarteten Instanz und fehlende Dripstone-3D-Modelle allgemein. Dripstone geprüft: das gebündelte Pack (`3D Default` v1.15.0, laut Modrinth-API auch die aktuell neueste Version) enthält **keine** Dripstone-Modelle in keiner Versions-Overlay - kein Bug, das Pack deckt den Block schlicht nicht ab. Fehlende Textur in der Launcher-Instanz dagegen echt nachvollziehbar: `resourcepacks-bundle/` (wie `mods-bundle/` aus Phase 4) wurde bis dahin nur einmalig von Hand in die Instanz kopiert - kein Code löscht das, aber sobald die Instanz neu angelegt/zurückgesetzt wird oder der manuelle Schritt schlicht vergessen wird, fehlt die Datei wieder. Auf Nutzerwunsch ("der Einfachheit halber") direkt behoben statt bis Phase 6 zu warten: neues `launcher/src/main/launch/bundleSync.ts`, kopiert bei **jedem** Start sowohl `mods-bundle/*` nach `<instance>/game/mods/` als auch `resourcepacks-bundle/*` nach `<instance>/game/resourcepacks/` (Aufruf in `ipc/handlers.ts` direkt nach `installFabricLoader`, neuer Progress-Stage `'bundles'`). Bewusst nur additiv (kopiert/überschreibt nur die Bundle-eigenen Dateien, löscht nie etwas anderes aus diesen Ordnern - falls der Nutzer selbst zusätzliche Mods/Packs reingelegt hat, bleiben die unangetastet). `options.txt` (welche Packs aktiviert sind) bleibt bewusst unangetastet - das Aktivieren im Spiel bleibt ein einmaliger manueller Schritt, wie bei jedem Vanilla-Resourcepack; nur das *Vorhandensein* der Datei ist jetzt garantiert. Pfadauflösung über `app.getAppPath()` (aktuell = `launcher/`-Ordner, da noch keine electron-builder-Paketierung existiert - muss bei echter Paketierung in Phase 6 nochmal gegengeprüft werden, siehe `extraResources`).

**Zweiter Nachtrag, gleicher Tag:** Nach dem Bundle-Sync-Fix meldete der Nutzer, gar keine der eigenen Mod-Features mehr im Spiel zu sehen (weder alt noch neu). Ursache diesmal deutlich gravierender als der reine Sync: `launcher/mods-bundle/tntsallin1client-0.1.0.jar` war ein **4 KB großer Stub-Build vom 23. Juli** (Phase 1/2-Ära) - der tatsächliche, an dem Tag aktuelle Gradle-Build in `mod/build/libs/` war 94 KB, mit sämtlichen Phase-5-Features. Die ganze restliche Session über wurde über den Launcher also (falls überhaupt darüber statt über `gradlew runClient` getestet) ein fast leerer Mod geladen, weil niemand den Bundle-Jar nach den vielen Rebuilds von Hand aktualisiert hatte - ein struktureller Fallstrick des ursprünglichen "einmal reinkopieren"-Musters, nicht nur ein einmaliger Fehler.

Behoben nicht durch einen erneuten manuellen Copy (der würde beim nächsten Rebuild sofort wieder veralten), sondern strukturell: `bundleSync.ts` bekommt eine zweite Funktion `syncOwnModJar`, die bei **jedem** Start direkt aus `mod/build/libs/` den jeweils neuesten `.jar` (nach `mtime`, `-sources.jar`/`-dev.jar` explizit ausgeschlossen - letzteres ist Looms unangepasster Intermediary-Jar, der so nicht lauffähig wäre) nach `<instance>/game/mods/` kopiert und dabei überschreibt. `mods-bundle/` ist damit nur noch für die echten Drittanbieter-Mods (Sodium/Lithium/Fabric API) zuständig, die sich nicht ständig ändern; der eigene Mod-Jar kann in diesem Ordner gar nicht mehr veralten, weil er dort gar nicht mehr die Quelle ist. Die alte 4-KB-Stub-Datei aus `mods-bundle/` lokal gelöscht (git-ignoriert, keine Repo-Änderung). Wichtige Konsequenz für den Workflow: **`gradlew build` muss vor jedem Launcher-Test gelaufen sein**, sonst startet der zuletzt gebaute Stand, nicht der aktuelle Quellcode.

**Dritter Nachtrag, gleicher Tag:** Nach dem Jar-Fix startete Minecraft zwar, aber Fabric Loader brach mit "Incompatible mods found!" ab: `tntsallin1client 0.1.0` verlangt laut `fabric.mod.json` (`"continuity": ">=3.0.1-beta.1"`) eine Continuity-Version, die fehlte. Ursache: Continuity ist in `mod/build.gradle.kts` als `modImplementation("maven.modrinth:continuity:3.0.1-beta.1+1.21.11")` eingebunden (Phase 5f) - das funktioniert automatisch in `gradlew runClient`, weil Loom die Mod-Abhängigkeit dort selbst auflöst, aber der Launcher weiß nichts von Gradle-Abhängigkeiten und braucht die Jar-Datei genau wie Sodium/Lithium/Fabric API physisch in `mods-bundle/`. Das wurde in Phase 5f schlicht vergessen; ist erst jetzt aufgefallen, weil dies der erste echte Test über den eigenen Launcher (statt `runClient`) seit Einführung von Continuity war. Exakt passende Version `3.0.1-beta.1+1.21.11` per Modrinth-API-Version-Endpoint geladen, SHA-1 gegen den gemeldeten Hash verifiziert (`51d34c3f2035db0c7fca321b2d94f01b5167707c`, 997.125 Bytes, exakt passend), liegt jetzt in `launcher/mods-bundle/`. Restliche `depends`-Einträge aus `fabric.mod.json` gegengeprüft (`fabricloader`/`minecraft`/`java` kommen vom Launcher selbst, `fabric-api` war schon vorhanden) - keine weiteren fehlenden Hard-Dependencies.

**5g — FPS-Anzeige (`hud/FpsCounterHud.java`):** Eigenes `HudElement`, exakt nach dem 5a/5b-Muster (unabhängig togglebar, per HUD-Editor verschieb-/skalierbar). Nutzt `Minecraft#getFps()` (dieselbe Methode, die 5ds Quick-Info-Block schon verwendet, aus den decompilierten 1.21.11-Quellen bestätigt statt geraten). Default-Position oben rechts, unter dem Material-Zähler.

**5h — Zoom (`zoom/ZoomHandler.java`, `keybind/ModKeyBindings.ZOOM`):** `GameRenderer#getFov()` liest `options.fov()` jeden Frame frisch aus (in den decompilierten Quellen verifiziert), also reicht es, den Options-Wert bei gehaltener Taste temporär zu überschreiben und beim Loslassen wiederherzustellen; Vanillas eigenes FOV-Lerp/Smoothing übernimmt den Rest. Taste standardmäßig unbelegt (gleiches Muster wie Inventar-Sortierung/Mod-Menü-Taste), zusätzlich per Mod-Menü-Toggle komplett abschaltbar.

**Bugfix (Nutzer-Feedback nach 5h, "Zoom-Taste gesetzt, aber Zoomen ging nicht"):** `ZOOM_FOV = 15` hat schlicht nie funktioniert - Vanillas `options.fov()`-ValueSet ist ein `OptionInstance.IntRange(30, 110)` (in `Options.java` verifiziert), `set(15)` wurde also stillschweigend abgelehnt und auf den 70er-Standardwert zurückgesetzt. Exakt derselbe Fehlertyp wie bei 5j (Fullbright/Gamma), den ich dort korrekt behandelt hatte, hier aber beim ursprünglichen Bauen übersehen - hätte durch einen kurzen Blick in `Options.java`s `fov`-Deklaration auffallen können, genau wie bei `gamma`. Gefixt mit demselben schon vorhandenen `OptionInstanceAccessor` (Validierung umgehen), jetzt echte Wirkung. Zusätzlich auf Nutzerwunsch: eigener **`menu/ZoomOptionsScreen`** mit Tasten-Rebind direkt im Mod-Menü, identisches "Klicken, dann Taste drücken, Escape entbindet"-Muster wie beim F3-System-Info-Key (`F3OptionsScreen`) - editiert dieselbe `ModKeyBindings.ZOOM`-KeyMapping wie Vanillas Steuerung-Menü, beide können nie auseinanderlaufen. Restlichen `.set(...)`-Aufrufe im Code geprüft: alle anderen setzen nur zuvor gültige, aus `get()` stammende Werte zurück - kein weiteres Vorkommen dieses Fehlertyps.

**Follow-up-Feature (Nutzer-Wunsch):** Während des Zoomens jetzt mit dem Mausrad stärker/schwächer reinzoomen. Vanilla nutzt das Mausrad während des normalen Spiels sonst zum Wechseln des Hotbar-Slots (`MouseHandler#onScroll`, privat, kein eigener Erweiterungspunkt) - neuer `mixin/MouseHandlerMixin` fängt das per `@Inject(HEAD, cancellable)` nur ab, wenn `ZoomHandler.isZooming()` wahr ist (sonst unverändert, Hotbar-Wechsel bleibt normal). Zoom-Stufe (`ClientConfig.zoomFov`, 2–50 begrenzt, 2 pro Mausrad-Kerbe) wird jetzt wie jede andere Einstellung gespeichert - man landet beim nächsten Zoomen wieder bei der zuletzt gewählten Stärke statt immer beim Standardwert. Hinweistext dazu im `ZoomOptionsScreen` ergänzt.

**5i — Eigenes Fadenkreuz (`crosshair/CustomCrosshair.java`):** Erstes Mal in dieser zweiten Runde, dass ein Mixin nötig war (nicht nur HudElement) - ein einfacher, farbiger "+" reicht zwar als eigener Overlay, würde aber zusätzlich zum, nicht statt des Vanilla-Fadenkreuz-Sprites gezeichnet. Deshalb ein zweiter `@Inject` in `GuiMixin` (neben dem Phase-2-Label), diesmal in `Gui#renderCrosshair` (private, `@At("HEAD")`, `cancellable = true`) - Methode/Signatur aus den decompilierten 1.21.11-Quellen bestätigt, nicht geraten. Bewusst vereinfacht: kein Angriffsindikator, keine Spectator-Entity-Ausnahme wie bei Vanilla - beides für ein "leicht"-Feature nicht nachgebaut. Farbe per Hex-Textfeld (6 Stellen, ohne "#") im eigenen Options-Screen mit Live-Vorschau-Swatch; Alpha-Byte beim Parsen immer fest auf `0xFF` gesetzt (die aus Phase 2 bekannte "bare RGB = unsichtbar"-Falle bewusst vermieden). Default-Farbe Weiß, damit das Zuschalten standardmäßig nichts sichtbar verändert.

**Nachtrag, deutlich später (`crosshair/CrosshairMode.java`, `CrosshairGrid.java`, `CrosshairPreset.java`, `menu/CrosshairOptionsScreen.java`, `menu/CrosshairTargetColorOptionsScreen.java`):** Nutzerwunsch, inspiriert von Dawn Clients Fadenkreuz-Editor (Referenzscreenshots beigelegt) - drei Dinge auf einmal: (1) Auswahl zwischen Vanilla-Standard, einer festen Formen-Bibliothek oder komplett selbst zeichnen; (2) Fadenkreuz-Größe unabhängig von der GUI-Skalierung einstellbar; (3) ein Modus, in dem das Fadenkreuz eine andere (frei wählbare) Farbe annimmt, während man ein Mob anvisiert, das man auch tatsächlich angreifen kann.

Vorab per Rückfrage geklärt: (a) statt einer fehleranfälligen "ist die Basisfarbe schon rötlich"-Heuristik einfach eine zweite, komplett unabhängig einstellbare Zielfarbe (Standard Rot) - robuster und dem Nutzer überlassen, falls die Basisfarbe selbst schon rot ist; (b) alle 13 Presets aus dem Referenzbild außer "Heart"; (c) 9x9-Raster fürs Selbst-Zeichnen (grob genug zum bequemen Anklicken).

Technisch: Presets UND selbst gezeichnete Fadenkreuze nutzen dieselbe Datenstruktur - ein 9x9-Pixel-Raster (`CrosshairGrid`, `boolean[][]`) plus eine gemeinsame Zeichenroutine. Runde Presets (Circle, Circle Dot) nicht von Hand aus 81 Boolean-Werten "gemalt", sondern über einen Abstand-zum-Zentrum-Schwellenwert generiert (`circleRing(radius)`) - deutlich weniger fehleranfällig als Pixel für Pixel per Auge zu schätzen; Quadrat-Presets analog über `squareRing(radius)` (`max(|dx|,|dy|) == radius`). Bewusst **keine** pixelgenaue Nachbildung der Dawn-Client-Icons versucht (aus einem kleinen Screenshot nicht seriös möglich) - Ziel war eine klar unterscheidbare Formen-Auswahl, kein Assets-Klon.

**Ziel-Farbe (Punkt 3):** `Minecraft.crosshairPickEntity` verwendet - exakt das Feld, das Vanillas eigene Angriffs-Anzeige (Schwert-Cooldown-Balken) schon nutzt, gefiltert auf `LivingEntity`, die kein Spieler ist. In den decompilierten Quellen nachgeprüft, dass das bereits korrekt reichweitenbegrenzt ist: `LocalPlayer#raycastHitResult` pickt Entities über `entityInteractionRange()` (inklusive einer eventuellen Item-spezifischen `DataComponents.ATTACK_RANGE`-Overrides für Reichweiten-Waffen) - "würde ein Linksklick treffen" kommt also gratis mit, keine eigene Abstandsrechnung nötig.

**Größe unabhängig von GUI-Skalierung:** HUD-Rendering läuft immer schon innerhalb einer Pose-Stack-Skalierung passend zur aktuellen GUI-Skalierung (bestätigt an jedem anderen skalierten HUD-Element in diesem Projekt, z.B. `KeystrokesHud`). "GUI-Skalierung ignorieren" rechnet das mit `1/guiScale` wieder heraus, bevor gezeichnet wird - `crosshairPixelSize` bildet dann echte Bildschirmpixel ab statt GUI-Skalierungs-Einheiten.

**UI-Aufteilung:** Der Options-Screen baut sich bei Moduswechsel (Vorlage ↔ Selbst-Zeichnen) komplett neu auf (`clearWidgets()` + `init()`), statt Widgets bedingt ein-/auszublenden - die beiden Bereiche (Vorlagen-Umschalter + kleine Vorschau vs. anklickbares 9x9-Raster) unterscheiden sich zu stark in Form/Höhe, um das sauber zu jonglieren. Die Ziel-Farbe bekam einen eigenen Unter-Screen (`CrosshairTargetColorOptionsScreen`) statt alles auf den ohnehin schon langen Haupt-Screen zu packen - gleiche "jedes Feature einen fokussierten Screen"-Konvention wie überall sonst in diesem Mod. Malen im 9x9-Raster funktioniert wie ein einfacher Pixel-Editor: der erste angeklickte Pixel entscheidet, ob der ganze Zug an/aus malt (Umkehren des ersten Pixel-Zustands), Ziehen wendet denselben Wert auf alle überstrichenen Zellen an.

**Erster Live-Test, fünf Punkte gemeldet (Screenshots beigelegt):**

1. **Square-Plus/Square-Plus-Dot falsch:** Die Kreuzarme endeten außen an der Box, statt durch sie hindurchzugehen. Ursache: die Arme wurden nur als kurze Ticks *außerhalb* des Ring-Radius gezeichnet. Fix: die Arme laufen jetzt über die komplette Rasterbreite/-höhe (bei Square Plus durchgehend, bei Square Plus Dot mit derselben Lücke um den Mittelpunkt wie bei Plus Dot), kreuzen den Box-Ring also mittendurch statt außen anzustoßen.

2. **Vorschau überdeckte den Auswahl-Button:** Bug gefunden - die Vorschau-Position wurde in `render()` *unabhängig* von der tatsächlichen Platzierung in `init()` neu berechnet und driftete dabei auseinander (fehlende Zeilenhöhe des Vorlagen-Buttons fehlte in der Rechnung), sodass die Vorschau direkt über dem Button landete. Fix: `previewX`/`previewY` (analog zu `gridX`/`gridY` fürs Zeichnen-Raster) werden jetzt einmalig in `init()` gesetzt und in `render()` nur noch gelesen - eine einzige Quelle der Wahrheit statt zweier auseinanderlaufender Berechnungen.

3. **Crosshair nicht exakt mittig:** Tieferliegender Bug in `CrosshairGrid#render` selbst (betraf also nicht nur die Vorschau, sondern auch das echte Fadenkreuz im Spiel) - jede Zelle wurde mit ihrer *oberen linken* Ecke an der Zielposition gezeichnet statt zentriert, wodurch das ganze Raster um ein halbes Pixel zu weit rechts/unten saß. Fix: `-pixelSize/2`-Korrektur beim Zeichnen.

4. **Zahlenfeld statt Schieberegler für die Größe:** Neue `IntSliderButton`-Klasse (auf Vanillas eigenem `AbstractSliderButton` aufgebaut, gleiche Optik wie jeder andere Vanilla-Options-Schieberegler) ersetzt das Eingabefeld.

5. **Back-Button im Zeichnen-Modus abgeschnitten:** Der Screen war im Custom-Draw-Modus (9x9-Raster + alles darunter) höher als der Bildschirm. Statt den Screen zu verkleinern (würde bei anderen Auflösungen wieder brechen) jetzt echtes Scrollen: `scrollOffset` wird bei jedem Mausrad-Scroll aktualisiert und der komplette Screen per `rebuild()` (`clearWidgets()` + `init()`) mit dem neuen Offset neu aufgebaut - `computeContentHeight()` spiegelt dafür dieselbe Y-Cursor-Rechnung wie `init()` (muss bei Layout-Änderungen synchron gehalten werden, entsprechend kommentiert). Scroll wird zuerst an Widgets unter der Maus durchgereicht (`super.mouseScrolled(...)` zuerst) - hovert man z.B. über einen der Umschalt-Buttons, ändert Scrollen weiterhin dessen Wert statt die Seite zu verschieben, wie überall sonst in Vanilla auch.

6. **`ColorPickerPanel` Hex-Feld/Swatch-Layout (betrifft alle drei Farbregler-Screens - Crosshair, Hitbox, Keystrokes):** Das Hex-Eingabefeld war so breit wie die ganze Zeile, wodurch die danebengesetzte Vorschau-Farbfläche mitten im Feld zu schweben schien. Fix: Hex-Feld + Vorschau-Fläche zusammen jetzt genauso breit wie ein einzelnes RGB-Feld (gleiche Spalte), dahinter (an derselben X-Position wie "Red"/"Green"/"Blue") steht jetzt auch "Hex" als Beschriftung.

Alle sechs Punkte behoben, Build grün - bitte erneut gegenchecken, insbesondere Scroll-Verhalten und ob das Fadenkreuz jetzt wirklich exakt mittig sitzt.

**Nachtrag, gleicher Tag - versucht und wieder verworfen:** Größen-Schritte unter 1 (0,25/0,5/0,75) gebaut (`DiscreteSliderButton`, `crosshairPixelSize` als `float`), nach kurzem Live-Test aber vom Nutzer als "zu buggy" wieder rausgeworfen und per `git revert` komplett zurückgerollt - Größe bleibt bei ganzen Zahlen 1-6 (`IntSliderButton`, `crosshairPixelSize` als `int`), wie vor diesem Versuch.

**Bugfix direkt danach - Absturz beim Start:** Der Revert allein reichte nicht. Die gespeicherte Config-Datei des Nutzers hatte durch den kurzen Zwischenstand `"crosshairPixelSize": 0.75` (Float) auf der Platte stehen; nach dem Revert ist das Feld wieder ein `int`, und GSON wirft beim Parsen eines Float-Werts in ein `int`-Feld eine `JsonParseException`. `ClientConfig#load()` hat bis dahin nur `IOException` abgefangen - die `JsonParseException` lief also ungefangen durch und crashte das ganze Spiel schon beim Start, noch bevor irgendein Menü sichtbar wurde. Zwei Fixes: (1) die konkrete Datei des Nutzers direkt auf einen gültigen Wert korrigiert, damit es sofort wieder läuft; (2) der eigentliche Bug behoben - `load()` fängt jetzt zusätzlich `JsonParseException` ab und fällt wie bei einem IO-Fehler sauber auf die Standard-Config zurück, statt das ganze Spiel mitzureißen. Das schützt jetzt generell gegen diese Fehlerklasse (jede zukünftige Feldtyp-Änderung, manuelle Bearbeitung oder Beschädigung der Config-Datei), nicht nur gegen diesen einen Fall.

**5j — Fullbright + Lichtlevel-Anzeige (`fullbright/FullbrightHandler.java`, `mixin/OptionInstanceAccessor.java`):** Zwei getrennte Teilfeatures aus einer Ideen-Zeile. Lichtlevel-Anzeige ursprünglich: die aus 5d bekannte Block-Licht-Logik (`getBrightness(LightLayer.BLOCK, pos)`), als eigenständiges HudElement statt F3-Zeile - bewusst nur die eine Zahl an der eigenen Position, kein voller In-World-Tile-Overlay über alle sichtbaren Blöcke (deutlich größerer Umfang, für "leicht-moderat" bewusst nicht gebaut) - **später komplett durch ein grafisches Overlay ersetzt, siehe unten.** Fullbright war überraschend kniffliger als erwartet: die Rendering-Pipeline für Licht läuft in 1.21.11 jetzt über einen GPU-Shader (`LightTexture`, UBO-Uniform statt der alten Java-Lookup-Tabelle), der `options.gamma()` direkt ausliest - der naheliegende "einfach `.set()` mit einem großen Wert aufrufen"-Trick greift aber nicht, weil `OptionInstance`s `UnitDouble`-ValueSet Werte außerhalb [0.0, 1.0] hart validiert und bei Verstoß kommentarlos auf den 0.5-Standardwert zurückfällt (in den decompilierten Quellen bestätigt). Lösung: neuer `@Accessor`-Mixin-Interface auf `OptionInstance` (erstes Mal in diesem Projekt Accessor statt `@Inject` - direkter Zugriff auf das private `value`-Feld, umgeht die Validierung komplett). Toggle-basiert (nicht gehalten wie Zoom), merkt sich beim Einschalten den vorherigen Gamma-Wert und stellt ihn beim Ausschalten wieder her - gleiches Save/Restore-Muster wie `ZoomHandler`. Bekannte, bewusst nicht behobene Einschränkung: wer während aktivem Fullbright manuell den Vanilla-Helligkeitsregler bewegt, überschreibt den erzwungenen Wert (Fullbright wirkt dann erst nach erneutem Aus-/Einschalten wieder).

**5j Redesign, deutlich später (`spawnoverlay/SpawnRisk.java`, `SpawnRiskCalculator.java`, `SpawnOverlayRenderer.java`, `menu/SpawnOverlayOptionsScreen.java`):** Nutzerwunsch: statt einer Zahl im HUD ein grafisches Overlay, das per Taste ein-/ausblendbar ist und direkt auf den Blöcken farbige "X"-Markierungen zeigt - rot für "hier spawnen Mobs immer", orange/gelb für "nur nachts", nichts für "nie" bzw. auf Luft. Zusätzlich gewünscht: im Mod-Menü wählbar, ob die Taste gehalten oder umschaltend funktioniert. Auf Nutzer-Bestätigung ersetzt das komplett die alte Text-Anzeige (nicht parallel dazu).

Zwei größere Recherchen vorher nötig, beide in den decompilierten 1.21.11-Quellen verifiziert statt angenommen:

1. **Die tatsächliche Spawn-Bedingung** (`Monster#isDarkEnoughToSpawn`, `SpawnPlacementTypes.ON_GROUND`, `NaturalSpawner.isValidEmptySpawnBlock`, `DimensionType.MonsterSettings`): Vanilla prüft (a) eine gültige Standfläche (Block darunter "isValidSpawn", meist sturdy + Lichtemission < 14), (b) freien Platz an der Position selbst und eine Zelle darüber (keine Kollisionsbox, keine Flüssigkeit, kein `PREVENT_MOB_SPAWNING_INSIDE`-Tag), (c) einen dimensionsabhängigen Block-Licht-Grenzwert (Overworld/End: **jedes** Blocklicht >0 verhindert Spawns, Nether: keine Blocklicht-Grenze) und (d) einen randomisierten Gesamt-Helligkeitstest gegen einen dimensionsabhängigen `IntProvider` (Overworld: `UniformInt(0,7)`, Nether: konstant 7, End: konstant 15). Für ein statisches Overlay wurde (d) deterministisch vereinfacht: der `IntProvider`-**Maximalwert** statt einer echten Zufallsstichprobe ("könnte es theoretisch passieren" statt einer echten Wahrscheinlichkeit, die ein X/kein-X-Symbol ohnehin nicht darstellen könnte) - der etablierte Ansatz, den auch die bekannten Community-Lichtlevel-Overlay-Mods so verwenden. Als Referenz-Mob dient `EntityType.ZOMBIE` (Zombie/Skelett/Spinne/Creeper/Enderman usw. teilen exakt dieselbe Platzierungsart und Prüf-Logik, siehe `SpawnPlacements`-Registrierung) - explizit **nicht** gedacht für Mobs mit eigenen Sonderregeln (Slime, Phantom, Drowned, ...).

2. **"Immer" vs. "nur nachts" ohne echte Zeit-Simulation:** Beide Werte werden unabhängig von der tatsächlichen aktuellen Uhrzeit berechnet, über `LevelReader#getMaxLocalRawBrightness(pos, skyDarken)` einmal mit `skyDarken=0` (Tag-Szenario) und einmal mit `skyDarken=11` (Nacht-Szenario) - der Nachtwert ist keine freie Annahme, sondern aus `Timelines.NIGHT_SKY_LIGHT_LEVEL = 4.0F` gegen den Tageswert `15.0F` zurückgerechnet (beide Konstanten in den 1.21.11-Quellen bestätigt; 15-4=11, exakt der aus älteren Versionen bekannte Sky-Darken-Wert für Vollnacht - das neue "Environment Attribute"-System hat also den klassischen Wert nur technisch neu verpackt, nicht geändert). Ergebnis: Tageswert ≤ Grenzwert → **immer** (rot); sonst Nachtwert ≤ Grenzwert → **nur nachts** (orange); sonst gar keine Markierung.

**Rendering, größter technischer Fund dieser Runde:** Vanillas eigenes neues `Gizmos`-Debug-Zeichensystem (schon aus 5l bekannt, dort für die Hitbox-Farbe genutzt) hat eine fertige Methode `Gizmos.billboardText(String, Vec3, TextGizmo.Style)` - eine farbige, immer zur Kamera ausgerichtete Textmarkierung an einer Weltposition. Exakt das, was ein "X"-Symbol braucht, ganz ohne eigenen `VertexConsumer`-Code. Einziger Haken: `Gizmos.addGizmo(...)` wirft eine `IllegalStateException`, wenn kein `GizmoCollector` aktiv ist - `Minecraft.java` öffnet diesen Collector aber über den **gesamten** `LevelRenderer#renderLevel`-Aufruf hinweg (`Gizmos.withCollector(...)` als Try-Ressource), und genau in dieses Zeitfenster fällt auch Fabrics eigener `WorldRenderEvents.BEFORE_DEBUG_RENDER`-Callback ("Use to draw lines, overlays similar to vanilla debug renders" - laut eigenem Javadoc). Ergebnis: ein simples `WorldRenderEvents.BEFORE_DEBUG_RENDER.register(...)`, darin direkt `Gizmos.billboardText(...)` aufgerufen, reicht - kein eigener Collector, keine eigene GPU-Zeichenlogik nötig, die komplette Bestehende Gizmo-Rendering-Pipeline wird einfach mitbenutzt.

**Performance-Grenzen, bewusst gesetzt:** Ein Scan über jeden geladenen Block wäre zu teuer für "jeden Frame". Stattdessen: Neuberechnung nur alle 10 Ticks (0.5s) und nur während das Overlay sichtbar ist, in einer festen Box von ±10 Blöcken horizontal/±4 vertikal um den Spieler (kein Konfigurationswert für den Radius - nicht angefragt, hätte das Feature unnötig aufgebläht).

**Bugfix, direkt nach dem ersten Live-Test gemeldet ("dreht sich, zu unübersichtlich"):** `Gizmos.billboardText("X", ...)` dreht sich immer zur Kamera - für eine einzelne Beschriftung sinnvoll, aber über ein dichtes Feld an Markierungen sieht das aus wie ein sich mitdrehendes/kippendes Gitter, sobald man sich bewegt (Screenshot vom Nutzer bestätigte genau das). Ersetzt durch zwei echte, flache `Gizmos.line(...)`-Segmente diagonal über die Blockoberfläche (Eck-zu-Eck) statt der Text-Billboard-Methode - reine Weltraum-Geometrie auf der Bodenebene, bleibt also unabhängig vom Kamerawinkel fix stehen. Nebeneffekt, der zum vom Nutzer gewünschten Aussehen (Referenzbild) passt: viele benachbarte X-Diagonalen teilen sich ihre Eckpunkte und ergeben zusammen optisch ein durchgehendes Rautengitter über den Boden - exakt das gewünschte Bild, nur jetzt ohne das Drehen.

**Nutzerwunsch, gleichzeitig:** Menü-Anzeigename von "Mob Spawn Overlay" auf **Lightlevel Overlay** geändert (Nutzer hat den Namen exakt so vorgegeben, deshalb 1:1 auch in der deutschen Übersetzung übernommen statt einer eigenen Übersetzung) - nur der im Mod-Menü sichtbare Text, nicht der interne Config-Key oder der Options-Screen-Titel (nicht angefragt).

**Konsequenz der Ersetzung:** `hud/LightLevelHud.java` komplett entfernt, inklusive HUD-Editor-Eintrag (`HudEditorScreen`) und der zugehörigen `ClientConfig`-Felder (`lightLevelHudEnabled`/`lightLevelHudLayout`) - ersetzt durch `spawnOverlayEnabled`/`spawnOverlayHoldMode`. Rebind-Screen (`SpawnOverlayOptionsScreen`) folgt exakt dem Zoom/Shulker-Muster; der Hold/Toggle-Umschalter nutzt denselben `CycleButton.booleanBuilder(...)`-Ansatz wie 5b's "Quelle: Gehaltenes Item/Festes Item"-Auswahl.

**Live vom Nutzer bestätigt:** Positionierung/Sichtbarkeit stimmte schon in der ersten Version, nur das kamera-drehende Billboard-Aussehen musste raus (siehe Bugfix oben).

**5k — Shulkerbox-Inhaltsanzeige, ursprüngliche Version (`tooltip/ShulkerContentsTooltip.java`, seither entfernt):** Wichtige Erkenntnis vor dem Bauen: Vanilla zeigt Shulkerbox-Inhalte in 1.21.11 bereits **teilweise** im Tooltip an (`ItemContainerContents` implementiert `TooltipProvider` und listet unaufgefordert die ersten 5 nicht-leeren Slots + eine "und N weitere"-Zeile). Erste Version hat deshalb bewusst nur die Items **ab dem 6.** als Text ergänzt, wenn `SHULKER_PREVIEW`-Taste gehalten wird - über Fabrics `ItemTooltipCallback` (kein Mixin nötig).

**Nutzer-Feedback, zwei Punkte:** (1) Die Taste ließ sich nicht belegen, und soll zusätzlich direkt im Mod-Menü einstellbar sein (wie bei Zoom/F3). (2) Statt der Text-Zeilen soll ein kleines Feld wie das echte Shulkerbox-Inventar angezeigt werden, in der Farbe der jeweiligen Box.

Zu (1): Die Registrierung selbst (`ModKeyBindings.register()`) sah beim Nachprüfen korrekt aus, kein konkreter Bug wie bei Zoom (dort war die Ursache eindeutig der IntRange-Clamp) gefunden - trotzdem direkt die gewünschte Alternative gebaut: neuer **`menu/ShulkerPreviewOptionsScreen`**, identisches Rebind-Muster wie bei Zoom/F3 (editiert dieselbe `ModKeyBindings.SHULKER_PREVIEW`-KeyMapping wie Vanillas Steuerung-Menü). **Vom Nutzer bestätigt: Rebind funktioniert.**

**Bugfix, danach gemeldet ("Keybind ändern klappt, Anzeige noch nicht"):** Die Anzeige-Bedingung prüfte `ModKeyBindings.SHULKER_PREVIEW.isDown()` - genau die schon aus Phase 5c bekannte Falle (siehe Projekt-Notiz "Keybind-while-screen-open quirk"), hier aber nicht dagegen gecheckt: Vanillas generisches Tasten-Tracking (`KeyMapping.set`/`.click`, wovon `isDown()`/`consumeClick()` lesen) läuft in `KeyboardHandler#keyPress` nur, wenn **kein Screen offen ist** (oder die Taste die F3-Modifier-Taste ist) - Shulkerbox-Hovern passiert aber ausschließlich in einem offenen Container-Screen (Inventar, Truhe, Shulkerbox selbst, ...), also wurde `isDown()` dort nie `true`. Gefixt nach demselben schon etablierten Muster wie bei 5c (`QuickSortUi`): `ScreenKeyboardEvents.afterKeyPress`/`afterKeyRelease` statt der generischen KeyMapping-Abfrage, diesmal für **jeden** `AbstractContainerScreen` registriert (nicht nur die Inventar-Screens wie bei 5c, da eine Shulkerbox in jedem Container liegen kann). Hält den Zustand in einem eigenen `keyHeldInScreen`-Flag, das bei jedem neu geöffneten Screen zurückgesetzt wird - da Fabrics Screen-Events beim Schließen eines Screens verworfen werden, könnte sonst ein noch gehaltener Key beim nächsten Screen fälschlich als "gehalten" hängen bleiben.

**Noch ein Bugfix, wieder danach gemeldet ("immer noch nicht"):** Zweite Ursache gefunden - `ScreenKeyboardEvents` deckt nur **Tastatur**-Bindings ab (`KeyMapping#matches(KeyEvent)` prüft explizit nur `Type.KEYSYM`). Für eine **Maustaste** braucht es die separate `matchesMouse(MouseButtonEvent)`-Methode und Fabrics `ScreenMouseEvents` (afterMouseClick/afterMouseRelease) - da der Rebind-Screen (wie Vanillas eigenes Steuerung-Menü) beides erlaubt, jetzt beide Event-Familien registriert. Nebenbei die Bedingung robuster gemacht: verlangt nicht mehr zwingend eine vorhandene `DataComponents.CONTAINER`-Komponente (eine leere/nie befüllte Box hat u.U. gar keine - zeigt jetzt trotzdem ein leeres Raster statt gar nichts). Zusätzlich zwei sparsame Diagnose-Log-Zeilen eingebaut (`logs/latest.log`, Suchbegriff "shulker-preview"), falls es doch noch nicht reicht - dann direkt danach schauen können statt erneut zu raten.

**Dritter Bugfix, diesmal auf expliziten Nutzerwunsch hin gefunden ("Anzeige sollte in allen Blöcken/Mobs funktionieren, mit denen man interagieren kann, auch Crafting-Table, Villager, etc."):** Beim Nachprüfen anhand der decompilierten 1.21.11-Quellen (`AbstractContainerScreen.java`, `AbstractRecipeBookScreen.java`) fiel auf, dass unser `@Inject` bis dahin auf `AbstractContainerScreen#render` zielte - eine Methode, die zwar von Truhen und `MerchantScreen` (Villager-Handel) direkt geerbt/aufgerufen wird, aber **nicht** von jedem `AbstractContainerScreen`. Alles, was von `AbstractRecipeBookScreen` erbt (Spieler-Inventar `InventoryScreen`, Crafting-Table `CraftingScreen`, sowie `AbstractFurnaceScreen` mit Furnace/Blast-Furnace/Smoker), überschreibt `render()` selbst und ruft darin `super.renderContents(...)` statt `super.render(...)` auf - unser Inject auf `render()` wurde für genau diese Screens also nie erreicht, obwohl der Rebind und die Grundlogik längst funktionierten. Fix: `AbstractContainerScreenMixin` zielt jetzt auf `renderContents` statt `render` - die eine Methode, die in beiden Vererbungspfaden tatsächlich aufgerufen wird (und in der `hoveredSlot` ohnehin frisch gesetzt wird) - damit greift die Vorschau jetzt einheitlich in jedem `AbstractContainerScreen`-Nachfahren: Truhen, Ender-Truhe, Shulkerbox selbst, Villager-Handel, Spieler-Inventar, Crafting-Table, Furnace-Varianten, Pferde-/Lama-Inventar usw. Per `javap`/decompilierten Quellen gegengeprüft statt geraten (Projekt-Konvention).

Zu (2) - deutlich größerer Umbau: Die alte Text-Erweiterung komplett entfernt, ersetzt durch ein echtes 27-Slot-Raster (`shulker/ShulkerPreviewRenderer.java`), das alle Items an ihrer tatsächlichen Position zeigt (leere Slots bleiben leer, über `ItemContainerContents#copyInto(...)` statt `nonEmptyItems()`, sonst würden Items nach vorne rutschen). Rahmen-/Hintergrundfarbe kommt aus `ShulkerBoxBlock#getColor()` → `DyeColor#getTextColor()` (bereits volle Deckkraft, kein Alpha-Byte-Risiko); die undyed Standard-Box (Farbe `null`) bekommt einen festen Lila-Ton nahe ihrer echten Textur.

**Nutzerwunsch danach, da inzwischen live bestätigt funktionierend:** Die 27 Slots waren nur ein durchgehender Farbfleck ohne Trennung. Dünne 1px-Gitterlinien zwischen den Slots ergänzt (`GRID_LINE_COLOR`), bewusst **nicht** in der Rand-/Box-Farbe (die kann je nach Dye stark variieren und würde bei hellen Farben kaum noch als eigenständige Linie erkennbar sein), sondern eine Stufe heller als die dunkle Hintergrundfläche - damit bleibt der Kontrast unabhängig von der jeweiligen Shulkerbox-Farbe konstant. Die beiden Diagnose-Log-Zeilen aus den vorherigen Bugfix-Runden gleichzeitig entfernt (samt dem dafür nötigen `lastLoggedHovering`-Feld) - jetzt live bestätigt, dass die Anzeige funktioniert, brauchte es sie nicht mehr, und ein Log-Eintrag pro Frame während des Hoverns wäre unnötiger Spam gewesen.

Kein saubere Erweiterungsstelle wie bei Bundles: Vanillas `ClientTooltipComponent.create(TooltipComponent)` ist ein **geschlossener** `switch` (nur `BundleTooltip`/`ActivePlayersTooltip`, `default -> throw new IllegalArgumentException`) - ein eigener `TooltipComponent`-Typ hätte zusätzlich einen zweiten Mixin in diese statische Interface-Methode gebraucht. Stattdessen komplett am vanilla-Tooltip-System vorbei gezeichnet, mit zwei Mixins statt einem: **`AbstractContainerScreenMixin`** liest am Ende von `AbstractContainerScreen#render` den gehoverten Slot aus (`@Shadow` auf `hoveredSlot`) und merkt sich das Item; **`ScreenMixin`** zeichnet das Raster am Ende von `Screen#renderWithTooltipAndSubtitles` - **nach** `GuiGraphics#renderDeferredElements()` (dort fließt der eigentliche Item-Tooltip erst ein), damit unser Raster nicht vom Vanilla-Tooltip überdeckt wird. Die Aufteilung auf zwei Mixins war nötig, weil der richtige Zeitpunkt zum Zeichnen (nach dem Tooltip-Flush) außerhalb von `AbstractContainerScreen` liegt, aber der gehoverte Slot nur dort bekannt ist. Zeichnet direkt über `GuiGraphics#renderItem`/`renderItemDecorations` (dieselben Aufrufe, die Vanillas eigenes Bundle-Tooltip intern nutzt) statt über die TooltipComponent-Abstraktion.

**5l — Anpassbare Hitbox-Farbe (`mixin/EntityHitboxDebugRendererMixin.java`, `menu/HitboxColorOptionsScreen.java`):** Kein saubere Erweiterungspunkt wie bei 5d dieses Mal - Vanilla zeichnet die F3+B-Hitbox über ein neues "Gizmos"-Debug-Zeichensystem (`net.minecraft.gizmos.Gizmos`/`GizmoStyle`, in 1.21.11 anstelle der älteren direkten Tesselator-Zeichnung), Farbe fest verdrahtet (`int i = bl ? grün : -1 (weiß)`) in einer privaten Methode (`EntityHitboxDebugRenderer#showHitboxes`). Die Methode zeichnet dieselbe Gizmos.cuboid-Signatur mehrfach für verschiedene Zwecke (Haupt-Hitbox, Fahrzeug-Marker, Augenhöhen-Linie, Enderdrache-Teile) - ein Ordinal-basierter `@Redirect` nur auf den ersten Aufruf wäre die "korrekte" chirurgische Lösung gewesen, aber deutlich fragiler/fortgeschrittener als alles bisher in diesem Projekt. Stattdessen bewusst einfacher gelöst: komplette Methode per `@Inject(at = "HEAD", cancellable = true)` abgefangen und nur Haupt-Hitbox-Box + Positions-Punkt selbst mit der eigenen Farbe neu gezeichnet, wenn `bl == false` (der Normalfall - `bl == true` ist ein seltener, nur für Entwickler relevanter "Vergleich mit der lokalen Server-Entity"-Modus, bleibt unangetastet). Bewusste Vereinfachung: die restlichen Vanilla-Indikatoren (Fahrzeug-Marker, Augenhöhen-Linie, Blickrichtungs-Pfeil, Enderdrache-Teile) fallen dabei weg, statt sie nur für eine Umfärbung komplett nachzubauen. Farbwahl-UI (Hex + Vorschau-Swatch) in einen gemeinsamen `menu/ColorPickerHelper` ausgelagert, da jetzt zum zweiten Mal fast identisch gebraucht (5i Fadenkreuz, jetzt 5l Hitbox) - jedes Feature behält trotzdem seinen eigenen Options-Screen, nur die kleine Hex-Parse-/Swatch-Logik ist geteilt.

**Nachtrag, deutlich später (`menu/ColorPickerPanel.java`):** Nutzerwunsch (betraf gleichzeitig 5i Fadenkreuz UND 5l Hitbox, da beide über `ColorPickerHelper` liefen): reine Hex-Eingabe reicht nicht, es sollte eine grafische Palette mit Schiebereglern geben, MS Paints "Farben bearbeiten"-Dialog als Referenzbild beigelegt. MS Paints Dialog ist HSL-basiert (großes Sättigung/Farbton-Quadrat + separater Helligkeits-Schieberegler) - stattdessen auf HSV aufgebaut (Sättigung/Wert-Quadrat + Farbton-Schieberegler), da Vanilla dafür mit `Mth.hsvToArgb(...)` schon eine fertige, geprüfte Umrechnung mitbringt (keine eigene Farbmathematik von Hand); für die Rückrichtung (Startfarbe/Hex-/RGB-Eingabe → Farbton/Sättigung/Wert) `java.awt.Color.RGBtoHSB(...)` verwendet - in diesem Projekt schon einmal für den Screenshot-Clipboard-Export akzeptiert, keine neue Abhängigkeit. Gleiche Idee wie im Referenzbild (2D-Fläche + 1D-Regler statt nur Zahlen tippen), nur mit einfacherer, bereits vorhandener Mathematik.

Neue Klasse `ColorPickerPanel` ist kein eigener `AbstractWidget`, sondern eine kleine Verwaltungsklasse nach demselben Muster wie `HudEditorScreen`s Drag-Boxen: sie erzeugt/registriert selbst die RGB- und Hex-Eingabefelder (echte `EditBox`-Widgets, über einen vom aufrufenden Options-Screen übergebenen `registerWidget`-Callback - `addRenderableWidget` ist `protected` und aus einer anderen Package nicht direkt aufrufbar, ein per `this::addRenderableWidget` im Screen selbst erzeugter Methodenverweis funktioniert aber problemlos, weil die Zugriffsprüfung schon beim Erzeugen des Verweises passiert), zeichnet Verlaufsquadrat/Farbton-Leiste/Marker selbst und bekommt `mouseClicked`/`mouseDragged`/`mouseReleased` vom Screen weitergereicht (identisches "erst eigene Logik prüfen, sonst an `super` durchreichen"-Muster wie bei den Tasten-Rebind-Screens). Verlaufsquadrat gezeichnet als viele schmale vertikale `fillGradient`-Streifen (Farbe oben je Spalte aus Farbton+Sättigung, Schwarz unten - Sättigung ändert sich mit der X-Position, Wert mit der Y-Position), Farbton-Leiste analog als gestapelte Vollton-Streifen.

Bidirektionale Synchronisierung (Quadrat/Leiste-Drag ↔ RGB-Felder ↔ Hex-Feld) brauchte einen `suppressResponders`-Schutz: `EditBox#setValue(...)` löst in den decompilierten Quellen bestätigt **immer** den eigenen Responder erneut aus (kein "stiller" Setter) - ohne die Sperre hätte jedes programmatische Zurückschreiben der Felder nach einem Dreh am Regler sofort wieder eine (unnötige, aber prinzipiell harmlose) Neuberechnung von Farbton/Sättigung/Wert aus dem gerade geschriebenen Text ausgelöst. `CrosshairOptionsScreen`/`HitboxColorOptionsScreen` selbst dadurch spürbar einfacher: bauen nur noch eine `ColorPickerPanel`-Instanz auf und reichen Render-/Maus-Events durch, die komplette Hex-Feld-Logik ist jetzt vollständig im Panel gekapselt.

**5m — Tastenanzeige/Keystrokes (`hud/KeystrokesHud.java`):** Klassisches Speedrun-/PvP-Montage-Overlay - Boxen für W/A/S/D/Shift/Space/LMB/RMB leuchten auf, wenn gedrückt. Über die Ideen-Zeile hinaus ("vielleicht auch mehr als das klassische...") zwei zusätzliche Boxen für Sprint-Taste und Q (Drop) ergänzt. Liest direkt Vanillas eigene `Options`-KeyMappings (`keyUp`/`keyLeft`/.../`keySprint`/`keyDrop`) statt eigene Tastenbelegungen zu definieren - was auch immer der Spieler sich fürs Bewegen/Angreifen belegt hat, leuchtet auf. Kein Mixin nötig, reines HudElement. Box-Breite pro Label an der tatsächlichen Textbreite berechnet statt einheitlich quadratisch (sonst würde "Shift"/"Sprint" aus einer W-großen Box rauslaufen). Default-Position unten rechts - die einzige HUD-Ecke, die noch frei war.

**Nachtrag, deutlich später (`menu/KeystrokesOptionsScreen.java`):** Zwei Nutzerwünsche zusammen. (1) Label "Q" auf "Drop" umbenannt (reine Textänderung in `buildRows`, die Breitenberechnung pro Label passt sich automatisch an, kein weiterer Code nötig). (2) Die Aufleucht-Farbe der aktiven Boxen einstellbar gemacht, über denselben `ColorPickerPanel` wie eben erst bei Fadenkreuz/Hitbox gebaut - neuer `KeystrokesOptionsScreen`, exakt gleiches Muster. Bewusst nur die aktive Farbe, nicht die inaktive Box-Farbe (nicht angefragt). Kleine Besonderheit: die aktiven Boxen waren schon immer bewusst halbtransparent (`0xC033CC33`, Alpha C0 statt voll) für einen "Leuchten"-Look; der Picker selbst erzwingt aber immer volle Deckkraft (0xFF) beim Speichern, wie bei jeder anderen Farbe in diesem Projekt. Kein Widerspruch: `ClientConfig#keystrokesActiveColor` speichert nur den RGB-Anteil verbindlich, `KeystrokesHud#render` setzt beim Zeichnen selbst wieder das feste `0xC0`-Alpha darüber (`(color & 0xFFFFFF) | 0xC0000000`) - der Leuchten-Look bleibt erhalten, nur der Farbton ist jetzt frei wählbar.

**Nachtrag, noch später (`hud/KeystrokeKey.java`, `menu/KeystrokesKeysOptionsScreen.java`):** Nutzerwunsch: einzelne Tasten der Keystrokes-Anzeige an-/ausschaltbar machen, direkt in der Form/Anordnung der echten Anzeige zum Anklicken ("die Buttons wie für die Anzeige der keystrokes anzeigen ... und dann durch drücken togglen"). Dafür zuerst ein Refactoring, das keine Verhaltensänderung selbst bewirkt: `KeystrokesHud` hatte bisher jede Box als anonymes `KeyEntry(String label, BooleanSupplier isDown)` direkt in `buildRows()` verdrahtet - ohne stabile Kennung pro Taste (nur den *aktuellen* Anzeigetext, der sich wie beim Q→Drop-Umbenennen schon einmal geändert hat) ließ sich keine persistente Konfiguration pro Taste sauber schlüsseln. Neues `enum KeystrokeKey` (W/A/S/D/Shift/Space/LMB/RMB/Sprint/Drop) mit Label **und** derselben Zeilen-/Spalten-Struktur als statische `ROWS`-Liste - jetzt die eine gemeinsame Quelle für sowohl die Live-Anzeige als auch den neuen Toggle-Screen, können also nie auseinanderlaufen.

`ClientConfig` bekommt `Map<String, Boolean> keystrokesKeyEnabled` (Schlüssel = `KeystrokeKey#name()`, fehlender Eintrag zählt als "an" - der Normalfall, bis jemand tatsächlich etwas abschaltet). `KeystrokesHud#buildRows` filtert jetzt pro Taste gegen diese Map und lässt komplett leere Zeilen (z.B. wenn sowohl Sprint als auch Drop aus sind) ganz weg statt eine Lücke zu lassen - `totalHeight` rechnet ohnehin schon mit der tatsächlichen Zeilenzahl, kein Sonderfall nötig.

Neuer `KeystrokesKeysOptionsScreen`: baut sich in `init()` exakt dieselbe Zeilen-/Box-Anordnung wie die echte Anzeige auf (aus `KeystrokeKey.ROWS`, zentriert, aber mit größeren, besser klickbaren Boxen als das kompakte HUD-Original), gezeichnet mit direktem `guiGraphics.fill`/`drawCenteredString` statt echten Widgets - gleiches Vorgehen wie schon bei `ColorPickerPanel`/`CrosshairGrid` (eigene kleine Verwaltungsklasse statt `AbstractWidget`, da hier eine unregelmäßige Anordnung statt eines gleichmäßigen Rasters gezeichnet wird). Deaktivierte Tasten bleiben sichtbar (nur gedimmt: dunklere Box, gedämpfte Textfarbe) statt zu verschwinden - sonst gäbe es bei einer komplett ausgeschalteten Zeile nichts mehr zum Anklicken, um sie wieder einzuschalten. Klick prüft nur, ob die Mausposition innerhalb einer Box-Bounding-Box liegt, kehrt den gespeicherten Wert um und speichert sofort - kein Ziehen/Mehrfachauswahl nötig, da hier (anders als beim 9x9-Fadenkreuz-Raster) nur zehn einzelne, unterschiedlich breite Boxen existieren. Erreichbar über einen neuen "Configure Keys"-Button im bestehenden `KeystrokesOptionsScreen`, oberhalb des "Back"-Buttons. `gradlew build` grün, noch nicht live getestet.

**5n — Screenshot-Chatnachricht mit Open/Copy (`screenshot/ScreenshotWatcher.java`, `screenshot/ScreenshotChatLink.java`):** Wichtige Erkenntnis vorher: Vanilla zeigt beim Screenshot schon eine Chat-Nachricht mit klickbarem Dateinamen, der die Datei öffnet (`ClickEvent.OpenFile`) - "Open" ist also technisch schon da, nur "Copy" (Bild in die Zwischenablage) fehlt.

**Bugfix ("wird mir nicht angezeigt"):** Die ursprüngliche Erkennung (`options.keyScreenshot.consumeClick()`, gleiche Technik wie bei den eigenen Tastenkombis) hat nie gegriffen - Ursache in `KeyboardHandler#keyPress` gefunden (decompilierte Quelle): der Screenshot-Zweig ruft `Screenshot.grab(...)` auf und danach sofort `return;`, **bevor** die Methode weiter unten `KeyMapping.click(key)` erreicht (das ist die Zeile, die den Klick-Zähler hochzählt, den `consumeClick()` abfragt). Vanilla behandelt F2 also als fest verdrahteten Sonderfall mit frühem Ausstieg - der Zähler für diese eine Taste bewegt sich dadurch nie, unabhängig davon, wie der Screenshot ausgelöst wurde. Deshalb komplett umgebaut: statt auf einen Tastendruck zu warten, pollt `ScreenshotWatcher` jetzt direkt den `screenshots/`-Ordner (alle 10 Ticks / 0.5s), merkt sich beim ersten Tick den aktuellen Dateibestand als Basislinie (damit nicht alle bereits vorhandenen Screenshots auf einmal gemeldet werden) und meldet jede Datei, die danach neu dazukommt - unabhängig von Taste oder Auslöser, dadurch robuster als der ursprüngliche Ansatz. Kurz geprüft, ob dieser Fehlertyp (Vanilla-Sonderfall mit frühem `return` vor der generischen Klick-Zählung) noch woanders im Projekt lauert: `keyDebugModifier` (F3, von `SystemInfoOverlay`/5d genutzt) hat keinen solchen frühen Ausstieg vor `KeyMapping.set`/`click` - bleibt also unbetroffen. Alle anderen Tasten-Abfragen im Projekt (`ZoomHandler`, `ShulkerContentsTooltip`, ...) beobachten ohnehin nur eigene, über `KeyBindingHelper` registrierte Tastenkombis, die nie einen Vanilla-Sonderfall haben können - `keyScreenshot` war der einzige Fall, in dem eine **fremde** (Vanilla-eigene) Taste beobachtet wurde.

**Umgebaut auf Nutzerwunsch (ursprünglich ein Popup-`Screen`, siehe Git-History):** Statt eines eigenen Fensters jetzt eine reine Chat-Nachricht im Format "Screenshot erstellt: <Datei> **[Open]** **[Copy]**" - Open grün, Copy blau, beide in eckigen Klammern, unterstrichen. "Open" bleibt ein normaler `ClickEvent.OpenFile` (identisch zu Vanillas eigenem Link). "Copy" **kann** kein normaler `ClickEvent` sein - Vanillas Klick-Aktionen sind ein fester Satz (OpenFile/OpenUrl/RunCommand/CopyToClipboard-nur-Text/Custom/...), und `Custom` schickt normalerweise nur ein Server-Paket (`ServerboundCustomClickActionPacket`), keine Möglichkeit für eigenen Client-Code. Der Trick: Vanillas **eigener** `ChatScreen` behandelt genau diesen `Custom`-Fall bereits selbst anders für eine interne Funktion (`ChatComponent.QUEUE_EXPAND_ID`, der "weitere gestapelte Nachrichten anzeigen"-Link) - `handleComponentClicked` prüft die ID **vor** dem Fallback aufs Server-Paket. Neuer `mixin/ChatScreenMixin` klinkt sich exakt an derselben Stelle mit einer eigenen ID (`tntsallin1client:screenshot_copy`) ein und fängt den Klick vorher ab. Der Dateipfad reist als NBT-`StringTag` im `Custom`-Payload mit, damit beim Klick klar ist, welcher (ggf. schon älteren) Chat-Zeile er zugehört. Eigentliches Kopieren unverändert: `ImageIO` liest die PNG als `BufferedImage`, `java.awt.Toolkit`/`Clipboard` legt sie als echtes Bild ab (dafür gibt's keinen Vanilla-Weg). `ScreenshotToastScreen` komplett entfernt, nicht nur ungenutzt gelassen.

**5o — Itemphysics (`mixin/ItemEntityRendererMixin.java`):** Wichtige Recherche vorher, analog zu 5j/5k/5n: der naheliegende "einen bestehenden Item-Physics-Mod bündeln" (wie in der Roadmap für dieses Feature vorgeschlagen, nach 5f-Muster) geht hier **nicht** wie bei Continuity, weil das klassische "ItemPhysic"-Mod (LGPL-2.1, github.com/CreativeMD/ItemPhysic, auf Modrinth für 1.21.11 gelistet) laut Modrinth-Metadaten `server_side: required` ist - echte Item-Physik (Items liegen flach am Boden, rollen, clippen nicht durch Blöcke) braucht die tatsächliche, serverseitig simulierte Entity-Position/-Rotation, die ein reiner Client-Mod nicht einfach erfinden kann, ohne gegen den vom Server gesendeten Zustand zu "kämpfen". Ein Bündeln hätte auf praktisch jedem Server (der das Mod nicht auch installiert hat - also fast jedem, inkl. genau der in der Roadmap explizit zugesicherten Lunar/Badlion-artigen Server-Kompatibilität) schlicht nichts bewirkt.

Erste Version: eine ehrliche, rein kosmetische Annäherung - liegen gelassene Items neigen sich zusätzlich zu Vanillas eigenem Bob/Spin (beides schon immer in `ItemEntityRenderer#submit` vorhanden) um einen festen, pro Item stabilen Kipp-Winkel auf X- und Z-Achse - abgeleitet aus demselben `bobOffset`-Wert, den Vanilla selbst schon für die Bob/Spin-Phase nutzt (kein neuer State nötig, keine echte Physik, rein optisch). Technisch die bisher komplexeste Mixin-Klasse: `submit(...)` ist eine `public`, nicht-private Methode mit mehreren Aufrufen an eine gemeinsame private `RandomSource`-Instanzvariable - direktes Einschleusen einer zusätzlichen Rotation mitten in die bestehende PoseStack-Transformationskette hätte einen handgeschriebenen JVM-Bytecode-INVOKE-Zielstring gebraucht (bei auch nur einem falschen Zeichen scheitert der Mixin beim Spielstart, nicht nur "funktioniert nicht" - ohne echten Spielstart hier nicht verifizierbar). Stattdessen wie bei 5l die komplette Methode per `@Inject(HEAD, cancellable)` abgefangen und **treu nachgebaut**, plus `@Shadow`-Zugriff auf das private `random`-Feld (erste Verwendung von `@Shadow` in diesem Projekt, neben `@Accessor` bei 5j und normalem `@Inject`) und einem expliziten `extends EntityRenderer<...>`, damit `super.submit(...)` am Ende weiterhin aufrufbar bleibt.

**Nutzer-Feedback, nachträglich:** "wenn es schon Physics heißt sollten die Items flach auf dem Boden liegen und sich nicht drehen (im Wasser sollten sie passend zur Wasserphysik in MC sich verhalten)" - der Kipp-Winkel allein hat sich zu Recht nicht wie "Physics" angefühlt, weil Vanillas Spin (kontinuierliche Y-Rotation) weiterhin lief. Umgebaut: liegt ein Item **auf festem Boden** (nicht in der Luft, nicht im Wasser), wird der Vanilla-Spin jetzt komplett gestoppt und das Item stattdessen flach hingelegt (90°-Drehung um die X-Achse) mit einem **festen** Yaw-Winkel aus `bobOffset` (ändert sich nie über die Zeit - "dreht sich nicht" heißt hier wortwörtlich das, nicht nur "dreht sich langsamer"). Schwebt/fällt das Item oder ist es im Wasser, greift jetzt unverändert Vanillas eigenes Bob/Spin-Verhalten (`ci.cancel()` wird dann gar nicht erst aufgerufen) - Vanillas Wasser-Auftrieb ist bereits real serverseitig simuliert und sieht dort schon richtig aus, die erzwungene Ruhe-Pose hätte dort nur falsch ausgesehen.

Technisch brauchte das zusätzlich Zugriff auf `itemEntity.onGround()`/`.isInWater()` - beides existiert nur auf der lebenden Entity, nicht auf `ItemEntityRenderState`, und `submit(...)` bekommt nie die lebende Entity (genau der Sinn der Render-State-Trennung: Rendering soll ohne die Live-Entity auskommen). Deshalb ein zweites Mixin-Paar: `ItemEntityRenderStateMixin` (fügt `ItemEntityRenderState` zwei neue `@Unique`-Felder hinzu - erste Verwendung von `@Unique` in diesem Projekt) plus `ItemPhysicsStateAccess` (Duck-Interface, über das `ItemEntityRendererMixin` die Felder liest/schreibt) und ein neuer `@Inject(TAIL)` in `extractRenderState(...)`, der `onGround`/`isInWater` genau wie Vanillas eigenes `bobOffset` einmal pro Frame von der Entity auf den Render-State kopiert. Ob die 90°-Drehung das Item mit der Vorderseite nach oben oder unten hinlegt, ließ sich ohne echten Spielstart nicht verifizieren - falls es verkehrt herum liegt, ist das eine Ein-Zeilen-Vorzeichenänderung.

**Bugfix, direkt beim ersten echten Testlauf gefunden (Absturz "Incompatible mods found" bzw. `IllegalClassLoadError` in `logs/latest.log`):** `ItemPhysicsStateAccess` lag anfangs im selben Package wie die Mixin-Klassen (`com.tntsallin1client.mixin`) - das Package ist in `tntsallin1client.mixins.json` als exklusiv für Mixin reserviert deklariert. Mixin verbietet, dass eine **normale** (nicht `@Mixin`-annotierte) Klasse aus einem ihm gehörenden Package direkt referenziert wird (hier: der Cast `(ItemPhysicsStateAccess) itemEntityRenderState`) - Absturz schon beim Ressourcen-Laden, noch vor der Welt. `OptionInstanceAccessor` (5j) hat exakt dasselbe Cast-Muster und liegt auch im Mixin-Package, funktioniert aber, weil es selbst ein `@Mixin`-Interface mit `@Accessor` ist - für solche wird das direkte Referenzieren von Mixin explizit erlaubt (das ist ja der ganze Sinn von Accessor-Interfaces). `ItemPhysicsStateAccess` ist dagegen ein reines, nicht annotiertes Interface (kann keins sein, weil es zwei brandneue Felder statt eines existierenden Feldes zugänglich macht - `@Accessor` funktioniert nur für bereits vorhandene Felder). Fix: in ein eigenes Package `com.tntsallin1client.itemphysics` verschoben, außerhalb der Mixin-Package-Reservierung. Nebenbei einen jetzt ungenutzten `Mth`-Import aus `ItemEntityRendererMixin` entfernt (Rest vom alten Tilt-Code).

**5q — Anpassbare Blockumriss-Farbe (`mixin/BlockOutlineMixin.java`, `menu/BlockOutlineColorOptionsScreen.java`):** Nutzerwunsch, direkte Erweiterung von 5l ("ähnlich wie wir das für die Hitbox schon gemacht haben"). Gleiche Ausgangslage wie bei 5l: kein sauberer Erweiterungspunkt, Vanilla zeichnet den Block-Auswahl-Umriss über drei private Methoden in `LevelRenderer` (`extractBlockOutline`/`renderBlockOutline`/`renderHitOutline`), verifiziert in den decompilierten 1.21.11-Quellen statt geraten. `renderHitOutline` bekommt bereits alles Nötige übergeben (Ziel-Block-Position + Form über `BlockOutlineRenderState`, Kamera-relative Offsets, fertige Farbe/Linienbreite) - deshalb wie bei der Hitbox per `@Inject(HEAD, cancellable)` abgefangen und mit `ShapeRenderer.renderShape(...)` (demselben Aufruf, den Vanilla im Nicht-Debug-Zweig selbst nutzt) neu gezeichnet.

Ein Unterschied zu 5l bewusst gemacht: der Umriss ist normalerweise **halbtransparent** (`ARGB.black(102)`, ~40% Deckkraft) statt wie die Hitbox voll deckend - ein direkt vom `ColorPickerPanel` übernommener Wert wäre aber immer voll opak (der Picker erzwingt beim Speichern immer `0xFF`-Alpha, dieselbe Konvention wie überall sonst im Projekt). Deshalb wie schon bei 5m/Keystrokes gelöst: `ClientConfig#customBlockOutlineColor` speichert nur den RGB-Anteil verbindlich, der Mixin übernimmt weiterhin Vanillas eigenes Alpha-Byte aus dem übergebenen Farbwert (`i & 0xFF000000`) und ersetzt nur die RGB-Bits - der gewohnte halbtransparente Look bleibt erhalten (bzw. der volldeckende Look im seltenen Accessibility-"High Contrast"-Modus), nur der Farbton ist frei wählbar. Default-Farbe Schwarz (Vanillas eigener Farbton), damit das Zuschalten standardmäßig nichts sichtbar verändert. Options-Screen (`BlockOutlineColorOptionsScreen`) 1:1 nach dem `HitboxColorOptionsScreen`-Muster gebaut (identischer `ColorPickerPanel`), Menü-Eintrag direkt unter "Custom Hitbox Color" einsortiert. `gradlew build` grün, noch nicht live getestet.

**5r — Dunkles Inventar, per Rendering-Mixin versucht und wieder verworfen:** Nutzerwunsch: das sehr helle weiß-graue Vanilla-Inventar dunkler machen. Erster Ansatz (analog 5l/5o): `AbstractContainerScreenMixin` verdunkelt nach `renderBackground` das Panel per halbtransparentem `guiGraphics.fill(...)`-Overlay (ein einziger, immer aufgerufener Erweiterungspunkt, deckt alle Container-Typen ab, siehe Git-History für die Recherche dazu). Danach vier Live-Test-Runden mit dem Nutzer, jede mit einem neuen Detail-Problem: (1) die Creative-Inventar-Tab-Buttons lagen außerhalb der Overlay-Fläche, brauchten einen eigenen `CreativeModeInventoryScreenMixin`; (2) der Rezeptbuch-Button ist ein echtes Widget außerhalb von `renderBg`, brauchte `AbstractRecipeBookScreenMixin`; (3) Vanillas fest verdrahtete dunkelgraue Titel-Textfarbe verlor gegen das jetzt dunkle Panel ihren Kontrast, brauchte einen eigenen `renderLabels`-Rewrite - und zwar **pro Screen-Klasse einzeln**, weil mehrere Container (`InventoryScreen`, `CreativeModeInventoryScreen`, dazu noch nicht behobene wie Amboss/Leuchtfeuer/Villager-Handel) `renderLabels` jeweils eigenständig überschreiben; (4) Icons (Amboss-Hammer, Smithing-/Zaubertisch-Buch, Creative-Tab-Symbole) wurden vom Overlay mitverdunkelt - lösbar nur durch Sprite-genaues Neu-Zeichnen statt eines pauschalen Rechtecks (führte am Ende noch zu einem sichtbaren Kasten-Artefakt an den nicht-rechteckigen Tab-Sprites, ebenfalls gefixt).

Nach der vierten Runde hat der Nutzer selbst die richtige Frage gestellt: "kann man das über einen anderen Weg besser lösen?" - zu Recht, jedes einzelne der vier Probleme war eine Instanz desselben Grundfehlers: Kontraste/Formen/Text zur Laufzeit per Rendering-Overlay nachzubauen statt sie da zu lösen, wo Minecraft sie tatsächlich herholt (den Texturen selbst). Recherche danach: **[Default Dark Mode](https://github.com/nebuIr/Default-Dark-Mode)** (nebulr) - mit ~6,47 Mio. Downloads praktisch der Standard-Dark-Mode-Resourcepack der MC-Szene, seit 1.6 durchgehend gepflegt, explizite 1.21.11-Version vorhanden (Changelog erwähnt sogar einen eigenen Fix für "fabric creative page buttons" - exakt das Creative-Tab-Problem, an dem wir uns die Zähne ausgebissen hatten). Datei geladen und SHA-1 gegen Modrinths gemeldeten Hash verifiziert; Zip-Inhalt stichprobenartig geprüft - enthält tatsächlich jede einzelne Textur, die die Mixins oben zu treffen versuchten (`inventory.png`, `anvil.png`, `furnace.png`, `smithing.png`, `enchanting_table.png`, alle sieben Creative-Tab-Sprite-Varianten, Amboss-/Smithing-Error-Sprites, ...). Lizenz CC-BY-NC-SA-4.0 (nicht-kommerziell, Namensnennung, Weitergabe unter gleichen Bedingungen) - passt zum aktuellen Hobbyprojekt-Status, siehe Lizenz-Hinweis in der Roadmap.

**Entscheidung, komplett umgesetzt:** Auf Nutzerbestätigung ("yes so wird gemacht") den kompletten Mixin-Ansatz rückgängig gemacht statt ihn weiter zu flicken: `AbstractContainerScreenMixin` zurück auf seinen 5k-Stand (nur noch der Shulker-Preview-Capture-Inject), `InventoryScreenMixin`/`CreativeModeInventoryScreenMixin`/`AbstractRecipeBookScreenMixin` komplett gelöscht, `ClientConfig#darkInventoryEnabled` + Mod-Menü-Eintrag + Sprachdatei-Einträge entfernt, `tntsallin1client.mixins.json` bereinigt. Stattdessen `Default-Dark-Mode-1.21.11-2026.4.0.zip` unverändert in `launcher/resourcepacks-bundle/` abgelegt - automatisch bei jedem Start synchronisiert wie 3D Default, einmalig manuell unter Optionen → Ressourcenpakete zu aktivieren. `gradlew build` grün nach der Bereinigung, keine Restreferenzen auf die gelöschten Klassen (gegrept).

**Live vom Nutzer bestätigt: "deutlich besser"** - Resourcepack aktiviert und getestet, keine der zuvor gemeldeten Probleme (Kasten-Artefakte, verdunkelte Icons, unlesbarer Titel-Text) mehr aufgetreten. Die vorab per Pixelmessung befürchtete knappe Titel-Text-Kontrast-Frage hat sich in der Praxis nicht bestätigt - kein Nachbesserungsbedarf, 5r damit ohne einzige Zeile Rendering-Mixin-Code abgeschlossen. Bestätigt insgesamt die Entscheidung, das Problem auf Asset-Ebene statt per Code zu lösen (gleiches Muster wie 5f/5p).

**5g–5o jetzt alle gebaut (`gradlew build` grün) - kompletter zweiter Phase-5-Durchgang fertig, keiner davon live getestet.** Bei 5j, 5l und jetzt 5o besonders wichtig zu testen, da die riskantesten Mixins bisher (Accessor / komplette Methodenersetzung, nicht nur einfaches additives `@Inject`). 5n unabhängig davon gut zu testen, da rein additiv (kein Mixin) - Screenshot machen (F2), sollte kurz ein Popup mit den zwei Buttons zeigen. Wichtig dafür: die Launcher-Instanz (`%APPDATA%\tntsallin1client-launcher\instances\default\game\mods\`) hat noch den alten Mod-Jar-Stand von Phase 4 (23.07., 4 KB) und **kein Continuity** - beides seither nicht mehr synchronisiert, weil bisherige Live-Tests über `runClient` liefen. Für einen Test über den echten Launcher müsste `mod/build/libs/tntsallin1client-0.1.0.jar` frisch reinkopiert werden (und für 5f zusätzlich Continuity). Automatisiert wird das erst mit Phase 6.

**5e — Ingame-Menü (`menu/`):** `ClientMenuScreen` (Haupt-Menü, nur An/Aus-Schalter pro Feature) + eigene Options-Screens pro Feature mit Detail-Einstellungen (`MaterialCounterOptionsScreen`, `CoordinatesHudOptionsScreen`) — bewusst **kein** gemeinsamer Options-Screen für alle Features (Nutzer-Feedback: "jede einzelne mod braucht ein Options-Menü, sonst wird's unübersichtlich"). Erreichbar über eigene Tastenkombi (unbelegt) und über einen Button im Pause-Menü, der exakt die alte Position von "Speichern und zum Titelbildschirm" einnimmt (der Button wird stattdessen nach unten verschoben) — implementiert nicht über einen Mixin in `PauseScreen`s privaten Layout-Code, sondern sucht nach dem fertigen Screen-Aufbau den vollbreiten (204px) Button mit der größten Y-Koordinate (robuster gegen Versions-Updates). Zusätzlich: **HUD-Editor** (`HudEditorScreen`) — Drag&Drop zum freien Verschieben, Ecke ziehen zum Skalieren, für Koordinaten-HUD und Material-Zähler. Funktioniert als Screen mit no-op `renderBackground()`, weil Minecraft den HUD-Layer unabhängig davon rendert, ob ein Screen offen ist (nur der Screen selbst dimmt normalerweise ab) — dadurch bleiben die echten, live laufenden HUD-Elemente unter den eigenen Zieh-Griffen sichtbar, ganz ohne Duplizieren der Render-Logik.

**5e-Nachbesserung (nach 5o, Nutzer-Feedback):** Mit 5g–5o zusammen hatte `ClientMenuScreen` 16 Zeilen bei festen, nicht scrollbaren Y-Koordinaten — bei GUI-Skalierung 3 oder höher (üblich auf 1080p+) hätte das die unteren Einträge (u.a. den "Fertig"-Button!) unerreichbar unten aus dem Bildschirm geschoben, ganz ohne Möglichkeit hinzuscrollen. Umgebaut auf eine echte scrollbare Liste (`ContainerObjectSelectionList`, dieselbe Basisklasse, die Vanillas eigenes Steuerung-Menü für seine Tastenbelegungs-Zeilen nutzt — Mausrad/Scrollbar/Tastatur-Navigation kommen dadurch automatisch mit, kein eigener Code nötig). Der "Fertig"-Button bleibt bewusst außerhalb der Liste fest am unteren Rand (typisches Options-Screen-Muster), alles andere (inkl. "Verbundene Texturen" und der HUD-Editor-Button) ist jetzt eine scrollbare Zeile. Nebenbei die bisher 16 fast identischen Widget-Erzeugungs-Blöcke auf zwei kleine Helper-Methoden (`addToggleRow`/`addButtonRow`) reduziert.

**5e-Nachbesserung, deutlich später (Sprachdateien):** Nutzerwunsch: die runden Klammern mit Zusatz-Hinweisen hinter den Mod-Namen (z.B. "Zoom (Hold Key)", "Custom Hitbox Color (F3+B)", "Item Physics (Rest Flat, No Spin)") raus - fand die Liste dadurch unübersichtlich. Betraf sechs Einträge (`menu.zoom`, `menu.shulker_preview`, `menu.hitbox_color`, `menu.screenshot_toast`, `menu.item_tilt`, `menu.connected_textures`) in beiden Sprachdateien, plus in der deutschen Version zusätzlich `menu.keystrokes` ("Tastenanzeige (Keystrokes)" - dort war die Klammer nur eine Übersetzungshilfe, kein Bedienhinweis, aber aus Konsistenzgründen ebenfalls entfernt). Reine Textänderung, kein Code betroffen.

**5e-Nachbesserung, direkt danach:** Nutzerwunsch: jeder einzelne Options-Screen soll oben seinen Namen als Text zeigen, genau wie `ClientMenuScreen` oben "Client Mods" zeigt. Jeder Screen hatte über den Konstruktor ohnehin schon einen `title` (Minecrafts `Screen`-Basisklasse verlangt den immer), nur gerendert wurde er bisher nirgends außer im Hauptmenü selbst - `Screen#render` zeichnet den Titel nicht automatisch, das macht jeder Screen selbst. Betraf alle elf Feature-Options-Screens (`CoordinatesHudOptionsScreen`, `CrosshairOptionsScreen`, `CrosshairTargetColorOptionsScreen`, `F3OptionsScreen`, `HitboxColorOptionsScreen`, `HudEditorScreen`, `KeystrokesOptionsScreen`, `MaterialCounterOptionsScreen`, `ShulkerPreviewOptionsScreen`, `SpawnOverlayOptionsScreen`, `ZoomOptionsScreen`) - bei den meisten schlicht `guiGraphics.drawCenteredString(this.font, this.title, this.width / 2, 12, 0xFFFFFFFF)` ergänzt (identische Position/Farbe wie im Hauptmenü). Zwei Sonderfälle: `ZoomOptionsScreen` hatte an y=40+Zeilenabstand schon einen Scroll-Hinweistext - Titel kam einfach oben drüber. `HudEditorScreen` hatte seinen Hinweistext direkt bei y=8 sitzen, wo jetzt der Titel steht - Titel auf y=6 gesetzt, Hinweistext eine Zeile tiefer verschoben, damit beide nicht überlappen. Bei `CrosshairOptionsScreen` (dem einzigen scrollenden Screen) bleibt der Titel bewusst außerhalb der Scroll-Berechnung - er wird jeden Frame separat an fester Position gezeichnet, verhält sich also automatisch wie eine fixierte Kopfzeile über dem scrollenden Inhalt.

**5f — Verbundene Texturen (`build.gradle.kts`, `ClientMenuScreen`):** Bewusst **nicht** selbst gebaut, sondern [Continuity](https://github.com/PepperCode1/Continuity) als `modImplementation`-Abhängigkeit gebündelt (LGPL-3.0-only, wie von der Roadmap für genau diesen Fall vorgesehen: "als Vorbild/Abhängigkeit, nicht neu erfinden"). Nur `3.0.1-beta.1+1.21.11` unterstützt unsere MC-Version (Beta-Status laut Continuitys eigenem Versionsschema — Risiko, bisher aber stabil vom Nutzer bestätigt). Continuity bringt ein fertiges "Default Connected Textures"-Resourcepack mit (Glas/Sandstein/Bücherregale) — keine eigene Pixel-Art nötig. Mod-Menü-Toggle nutzt Continuitys öffentliche `ContinuityFeatureStates`-API direkt (kein eigenes Config-Feld, Continuity persistiert selbst). **Wichtig für Ersteinrichtung:** Nutzer muss einmalig über Optionen → Ressourcenpakete "Default Connected Textures" (+ "Glass Pane Culling Fix" für Glasscheiben) aktivieren — unser Toggle schaltet nur, ob ein aktives CTM-Pack angewendet wird, nicht die Resourcepack-Auswahl selbst. **Live vom Nutzer bestätigt:** verbundenes Glas funktioniert.

**Nebenbei:** Das Phase-2-"Beweis, dass der Mixin läuft"-Label ("TNT's All-In-1 Client (Mixin active)") ist jetzt über den Mod-Menü-Toggle "Client Name Label" ausschaltbar, statt fest verdrahtet zu sein. Das Mod-Menü selbst zeigt jetzt oben eine Titelzeile ("Client Mods"/"Mod-Menü"), wie es bei Vanilla-Menüs (Pause, Optionen) auch üblich ist — vorher war der Titel nur intern für Vorlesefunktion gesetzt, aber nie tatsächlich gezeichnet.

## Phase 4 — Zwischenstand
Neu in `launcher/src/main/launch/`: `fabricMeta.ts` (ruft `meta.fabricmc.net/v2/versions/loader/{mcVersion}` ab, wählt die aktuell stabile Loader-Version, holt dann `.../profile/json` — das offizielle Fabric-"Launcher-Profil"-Format, dieselbe Quelle, die auch der offizielle Fabric-Installer nutzt) und `fabricInstaller.ts` (lädt die darin gelisteten Loader-Libraries als Maven-Koordinaten herunter, hängt sie an die Vanilla-`libraryPaths` an, überschreibt `mainClass` auf `net.fabricmc.loader.impl.launch.knot.KnotClient` und übernimmt den zusätzlichen JVM-Arg `-DFabricMcEmu=...`, den Fabric zur Laufzeit braucht um zu wissen, welche Klasse eigentlich gestartet werden sollte). Der Vanilla-Client-Jar selbst bleibt unangetastet — Fabric patcht zur Laufzeit über einen eigenen Classloader, ersetzt keine Datei. `ipc/handlers.ts` ruft `installFabricLoader` jetzt nach `installVersion` auf, bevor Classpath/Launch-Args gebaut werden; legt dabei auch `<instance>/game/mods/` an.

`downloader.ts` robuster gemacht: wenn die Fabric-Meta-API für eine Library keinen SHA-1 mitliefert (betrifft nur `fabric-loader` und `intermediary` selbst — alle anderen Libraries haben SHA-1), wird beim erneuten Start nur noch auf Dateiexistenz statt auf Hash geprüft, damit nicht bei jedem Play-Klick neu heruntergeladen wird.

Mod-Jars (eigener Mod + Fabric API + Sodium + Lithium) lagen fertig heruntergeladen mit gegen Modrinth verifiziertem SHA-1 in `launcher/mods-bundle/` (git-ignoriert, kein Source) bereit:
- `tntsallin1client-0.1.0.jar`
- `fabric-api-0.141.5+1.21.11.jar`
- `sodium-fabric-0.8.13+mc1.21.11.jar`
- `lithium-fabric-0.21.4+mc1.21.11.jar`

**Verifiziert (headless, ohne GUI):**
- `npm run typecheck` und `npm run build` sauber.
- Smoke-Test (temporäres Skript, nicht Teil des Repos) gegen die echte Fabric-Meta-API: stabile Loader-Version `0.19.3` für `1.21.11` ermittelt, `profile/json` geholt (`mainClass`, extra JVM-Arg, leere `game`-Args wie erwartet, 8 Libraries), alle 8 Libraries heruntergeladen, 6 davon SHA-1-verifiziert, die restlichen 2 (`fabric-loader`, `intermediary` — beide liefern laut echter API-Antwort keinen Hash) als vorhanden bestätigt.
- Fabric-API/Sodium/Lithium-Download-URLs nicht geraten, sondern live über die Modrinth-API für `1.21.11`+`fabric` abgefragt, heruntergeladen und der SHA-1 gegen die von Modrinth gemeldeten Hashes verifiziert (siehe oben).

**Vom Nutzer live bestätigt:** Jars in `<instance>/game/mods/` kopiert, "Play" gestartet — Start lief über Fabric Loader statt Vanilla. Alle drei "Fertig, wenn"-Kriterien der Roadmap erfüllt:
- Sodium-Renderer sichtbar in F3 ✅
- Lithium in der von Fabric Loader beim Start geloggten Mod-Liste bestätigt ✅ (kein eigener F3-Indikator, da Lithium reine Spiellogik statt Rendering optimiert — anders als Sodium keine eigene HUD-Zeile)
- Eigener Mod-Text ("TNT's All-In-1 Client (Mixin active)") oben links im HUD sichtbar ✅

Nebenbei richtiggestellt: Der "Mods"-Button im Pause-Menü kommt nicht von Fabric Loader/API selbst, sondern von der separaten Mod-Menu-Mod (nicht installiert) — Mod-Liste stattdessen über die Start-Log-Zeile "Loading N mods: ..." bestätigt.

## Phase 3 — Zwischenstand
`launcher/` angelegt: Electron 43 + electron-vite 5 + TypeScript 7 + React 19, Ordnerstruktur wie in der Roadmap (`src/main/{auth,launch,ipc}/`, `src/preload/`, `src/renderer/`, plus `src/shared/` für Typen/IPC-Kanalnamen, die von allen drei Seiten geteilt werden). Bewusst keine zusätzlichen Abhängigkeiten wie `electron-store` oder MSAL — Auth-Kette ist handgeschriebenes `fetch` gegen die dokumentierten Endpunkte (Lernziel laut Roadmap), Token-Cache ist eine simple JSON-Datei in `userData`.

**Was echt funktioniert (unabhängig vom Mojang-Blocker):**
- Auth-Schritte 1–3 komplett real implementiert: Microsoft OAuth2 mit PKCE über einen Loopback-HTTP-Server auf einem ephemeren Port (passt zur in Azure registrierten bloßen `http://localhost`-Redirect-URI, die Microsoft laut Doku für Desktop-Clients gegen jeden Port matcht), System-Browser-Login via `shell.openExternal` (kein eingebettetes WebView, damit die App nie Microsoft-Zugangsdaten sieht), danach Xbox-Live-User-Auth und XSTS-Autorisierung (`xboxLive.ts`) inkl. Klartext-Fehlermeldungen für die bekannten XSTS-Fehlercodes (kein Xbox-Profil / Kinderkonto ohne Zustimmung).
- Komplette Download/Start-Pipeline real gebaut und **gegen die echte Mojang-API/CDN smoke-getestet** (Version-Manifest für 1.21.11 geladen, Libraries nach OS gefiltert, eine echte Library heruntergeladen und SHA-1-verifiziert, Re-Download bei vorhandener gültiger Datei korrekt übersprungen, Classpath + JVM-/Game-Args mit aufgelösten Platzhaltern gebaut, `java` auf dem PATH gefunden).
- Sicherheits-Defaults gesetzt: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, schmale `contextBridge`-API im Preload.

**Was noch mit Dev-Mock läuft (Schritte 4–5, `minecraftAuth.ts`):** `login_with_xbox` + Profil-Abruf werden echt versucht; schlagen sie fehl (aktuell 403, da Azure-App noch nicht freigeschaltet), greift ein klar markiertes Platzhalterprofil (`DevPlayer`, `isMock: true`), das in der UI sichtbar als "Dev-Mock-Profil"-Badge markiert wird. Sobald Mojang freischaltet, entfällt der Fallback-Zweig ersatzlos — kein sonstiger Code muss sich ändern.

**Zwei Bugs beim ersten echten Login-Test gefunden und gefixt** (das Nachstellen dieser realen Testrunde war überhaupt erst möglich, weil `npm run dev` aus der Agent-Shell nicht bis zum GUI-Fenster kommt — dort ist `ELECTRON_RUN_AS_NODE=1` gesetzt, sehr wahrscheinlich eine bewusste Sandbox-Absicherung gegen automatisch aufpoppende Fenster; der Nutzer hat deshalb selbst in einem normalen Terminal getestet):
1. `unauthorized_client: The client does not exist or is not enabled for consumers`. "Supported account types" in Azure war korrekt auf "Nur persönliche Konten" gesetzt (nicht der naheliegende Verdacht) — tatsächliche Ursache war ein Tippfehler beim Abschreiben der Application (Client) ID in Phase 0: notiert war `6820dddd-8bc1-4fe6-ab9b-945a3a7d400a`, echt ist `6820dddb-...` (`b` statt `d` als achtes Zeichen). Fix: `launcher/src/main/config.ts` und die Notiz weiter unten auf die korrekte ID korrigiert.
2. Danach `invalid_request: The provided value for the input parameter 'redirect_uri' is not valid`. Ursache: Microsofts Any-Port-Matching für die registrierte bloße `http://localhost`-Redirect-URI greift nur, wenn die gesendete `redirect_uri` **keinen Pfad** hat — der Code sendete aber `http://localhost:{port}/callback`. Fix in `launcher/src/main/auth/msOAuth.ts`: `redirect_uri` ohne Pfad senden, Loopback-Server routet nicht mehr nach Pfad, ignoriert aber Anfragen ohne `code`/`error`-Parameter (verhindert, dass ein `favicon.ico`-Request den echten Callback wegschnappt).

Lehre aus beidem: GUIDs beim Abtippen aus dem Portal immer per Copy-Button kopieren, nie von Hand/Screenshot abschreiben.

**Verifiziert:**
- `npm run typecheck` (main+preload und renderer getrennt) sauber ohne Fehler, `npm run build` (electron-vite, alle drei Targets) erfolgreich.
- Pipeline-Smoke-Test (temporäres Skript, nicht Teil des Repos) gegen echte Mojang-Endpunkte durchgelaufen: Manifest/Version-Detail, OS-Filterung, Download+SHA-1, Classpath/Args, `java`-Verfügbarkeit — alles ✅.
- **Vom Nutzer live bestätigt (nach den beiden Bugfixes):** echter Microsoft-Login im System-Browser erfolgreich, danach automatischer Rücksprung ins Launcher-Fenster, Play-Screen mit Mock-Profil erreicht, "Play" gestartet — Minecraft 1.21.11 wurde heruntergeladen und **startet tatsächlich mit dem `DevPlayer`-Mock-Profil**. Damit ist die komplette Kette bis auf den echten Mojang-Zugriff (Schritte 4–5) einmal real durchgespielt.
- Minecraft-API-Freischaltungsantrag wurde erneut eingereicht (2026-07-18) — diesmal mit der korrekten Client-ID, da unklar war, ob die ursprüngliche Einreichung (2026-07-15) schon die verfälschte ID enthielt.

## Phase 2 — Erledigt
- Mixin-Infrastruktur aufgesetzt: `src/main/resources/tntsallin1client.mixins.json` (Package `com.tntsallin1client.mixin`, `compatibilityLevel: JAVA_21`), in `fabric.mod.json` unter `"mixins"` registriert. Fabric Loom (1.17.16, über den `net.fabricmc.fabric-loom-remap`-Plugin-Alias) erkennt die Config automatisch und übernimmt Annotation-Processing/Refmap ohne zusätzliche `build.gradle.kts`-Änderungen.
- `mixin/GuiMixin.java`: `@Mixin(Gui.class)` mit `@Inject(method = "render", at = @At("TAIL"))` in `net.minecraft.client.gui.Gui#render(GuiGraphics, DeltaTracker)` — die zentrale HUD-Render-Methode. Zielsignatur vorher direkt aus dem von `genSources` erzeugten, gemappten Vanilla-Quellcode verifiziert (Mojang-Mappings, kein Blindraten).
  - Zeichnet einmal pro Frame den Text "TNT's All-In-1 Client (Mixin active)" oben links ins HUD (`guiGraphics.drawString(...)`).
  - Loggt genau einmal (statisches Flag) eine Bestätigungszeile beim ersten Feuern, statt bei jedem Frame zu spammen.
- **Bug während des Tests gefunden und gefixt:** Farbwert `0xFFFFFF` an `drawString` übergeben wurde als 32-Bit-ARGB zu `0x00FFFFFF` (Alpha = 0) — `GuiGraphics.drawString` verwirft den Aufruf still, wenn `ARGB.alpha(k) == 0` (Quelle: `GuiGraphics.java`, Zeile 253). Fix: `0xFFFFFFFF` (volles Alpha) verwendet. Lehre: Farbwerte für GuiGraphics-Zeichenmethoden immer als ARGB inkl. Alpha-Byte angeben, nicht als reines RGB.
- Verifiziert im Dev-Client (`runClient`, zwei Durchläufe — erster zeigte den Bug, zweiter nach dem Fix bestätigt):
  - Log-Zeile `[tntsallin1client] Phase 2 mixin fired: injected into Gui#render.` erscheint beim Weltbeitritt ✅
  - Text ist sichtbar im HUD oben links ✅ (visuell vom Nutzer bestätigt)
- Mixin-Injection in eigenen Worten: Der Fabric-Mixin-Annotation-Prozessor webt zur Kompilierzeit Bytecode aus `GuiMixin` in die vom Spiel geladene `Gui`-Klasse ein — `@At("TAIL")` platziert den injizierten Code kurz vor dem `return` der `render`-Methode, sodass er nach allem Vanilla-HUD-Rendering, aber noch im selben Methodenaufruf läuft. Kein Overriding/Ersetzen der Originalmethode, sondern additive Injection in bestehenden Bytecode.

## Phase 1 — Erledigt
- Versionsrecherche direkt bei fabricmc.net/Modrinth (nicht auf die Roadmap-Planzahlen verlassen, siehe Risikohinweis #6):
  - **Minecraft hat tatsächlich auf ein jahresbasiertes Versionsschema gewechselt** (aktuell stabil: `26.2`, davor `26.1.x`). Der klassische `1.21.x`-Strang läuft aber parallel weiter und ist ebenfalls aktuell stabil (`1.21.11`).
  - Entscheidung: **`1.21.11` statt `26.2`** für den Mod verwendet. Grund: FabricMC's eigenes Referenz-Template für `26.2` verlangt **JDK 25**, `1.21.11` läuft mit dem bereits installierten **JDK 21** (Phase 0). Fabric API, Loader (`0.19.3`) und Sodium sind für beide Versionen aktuell — ein späterer Umstieg auf `26.x` ist somit ein reiner Versions-Bump, kein Rewrite.
- `mod/`-Projekt angelegt (Fabric Loom, Kotlin DSL wie in der Roadmap festgelegt, offizielle Mojang-Mappings statt Yarn — Yarn hat für `26.x` noch keine Builds, für `1.21.11` wären sie zwar da, aber das Template selbst nutzt inzwischen `officialMojangMappings()`):
  - `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties` (MC `1.21.11`, Loader `0.19.3`, Loom `1.17-SNAPSHOT`, Fabric API `0.141.5+1.21.11`).
  - Gradle Wrapper (Gradle `9.5.1`) aus FabricMC's offiziellem `fabric-example-mod`-Repo (Branch `1.21.11`) übernommen.
  - `com.tntsallin1client.TNTsAllIn1ClientMod` — Client-Entrypoint, Logger-Zeile beim Start, Log + Chat-Nachricht via `ClientPlayConnectionEvents.JOIN` beim Welt-Beitritt.
  - Bewusst **kein** Mixin-Beispiel und **kein** separates `client`-Sourceset eingebaut — Mixins sind laut Roadmap erst Phase 2, und das Projekt ist ohnehin reines Client-Mod (kein Server-Entrypoint nötig).
- `./gradlew build` lokal erfolgreich (Mojang-Mappings-Setup, Kompilierung, Jar+Sources-Jar) — `tntsallin1client-0.1.0.jar` liegt in `mod/build/libs/`. `genSources`- und `runClient`-Tasks sind vorhanden und einsatzbereit.

- IntelliJ: "Trust Project" bestätigt, Gradle-Sync durchgelaufen, "Minecraft Development"-Plugin (minecraft-dev, Fabric/Mixin-Support) installiert.
- `genSources` gelaufen (Vineflower, 6622 Klassen dekompiliert) — lesbarer Vanilla-Code für Navigation/Debugging verfügbar.
- `runClient` im Debug-Modus gestartet, Singleplayer-Welt erstellt. **Alle drei "Fertig, wenn"-Kriterien bestätigt:**
  - Dev-Client startet ✅
  - Breakpoint funktioniert ✅ (griff zuerst bei Zeile 19, dem `onInitializeClient`-Start — `client` ist dort noch nicht im Scope, das ist erwartet, da erst innerhalb der Lambda ab Zeile 22 verfügbar; nach Resume normal weitergelaufen)
  - Log-Zeile + Chat-Nachricht beim Welt-Beitritt ✅ ("TNT's All-In-1 Client loaded." im Chat, `[tntsallin1client] Joined a world.` im Log)

## Erledigt
- `git init` im Projektverzeichnis, Commits erstellt.
- `.gitignore` für das geplante Monorepo (mod/, launcher/, backend/) angelegt.
- JDK 21 (Temurin/Adoptium) installiert — `java -version` → 21.0.11.
- Node.js LTS installiert — `node -v` → v24.18.0.
- IntelliJ IDEA Community Edition installiert.
- Git war bereits vorhanden (2.51.2).
- Azure AD (Entra ID) Public-Client-App registriert: `TNTsAllIn1Client`, "Nur persönliche Konten", Redirect-URI `http://localhost` (öffentlicher Client), "Öffentliche Clientflows zulassen" = Ja.
  - Application (Client) ID: `6820dddb-8bc1-4fe6-ab9b-945a3a7d400a` (in einer früheren Version dieser Notiz stand fälschlich `6820dddd...` — Tippfehler beim Abschreiben aus dem Portal, hat in Phase 3 den Microsoft-Login mit `unauthorized_client: The client does not exist` blockiert, siehe Phase-3-Abschnitt)
  - Verzeichnis-ID (Tenant): `a3dd876f-c607-4a29-a300-73b9e7130cbb`
- Minecraft-API-Freischaltungsantrag unter aka.ms/mce-reviewappid abgeschickt (2026-07-15). **Antwortzeit unklar — laut Roadmap-Risikohinweis nicht darauf blockieren, direkt mit Phase 1 weitermachen.**
- Öffentliches GitHub-Repo angelegt und gepusht: [github.com/clenz-debug/TNTsAllIn1Client](https://github.com/clenz-debug/TNTsAllIn1Client)
- Projekt-Roadmap zusätzlich auf Englisch verfügbar: `Projekt_Roadmap.en.md`.

*Fertig, wenn:* Tools laufen (✅), Azure-App existiert (✅), Antrag ist abgeschickt (✅).

## Blocker-Log (Azure-Login, gelöst)
Beim Registrieren der Azure-App gab es mehrere ineinandergreifende Probleme, bis alles lief:

1. **Tenant-Routing-Fehler** (`AADSTS50020`/`AADSTS16000`, Tenant "Microsoft Services"): Login/Hintergrund-Widgets versuchten wiederholt, einen Token gegen einen fremden Phantom-Tenant zu holen, in dem der Account kein Mitglied ist. Trat bei mehreren Accounts gleich auf.
2. **Inkognito-Fenster** löste einen neuen Fehler aus (`AADSTS50058`, silent sign-in), da Inkognito-Modus Third-Party-Cookies blockiert, die Azure Portal fürs Silent-SSO braucht — zurück zu normalem Fenster.
3. **Systemzeit war nie synchronisiert** (`w32tm`: "nicht synchronisiert", Quelle "Local CMOS Clock") — nach manueller Sync über Windows-Einstellungen lief der Login sauber durch.
4. **Root Cause**: Der Account hatte schlicht noch **kein eigenes Entra-ID-Verzeichnis** (App-Registrierungen-Seite zeigte das explizit an). Der "Mandanten verwalten"-Self-Service-Weg war gesperrt, da er ohne bestehendes Abo/Lizenz keine Tenant-Erstellung erlaubt.
5. **Lösung**: Kostenloser Azure-Free-Account-Signup (Telefon-/Kartenverifizierung, keine Kosten im Free-Tier) hat automatisch ein echtes Verzeichnis provisioniert — danach lief die App-Registrierung ohne Probleme durch.

## Nächster Schritt
Phase 4 ist abgeschlossen, Phase 5 (5a–5f, 5p) ist abgeschlossen und live bestätigt. Der zweite Durchgang (5g–5o) ist jetzt komplett implementiert und gebaut, aber noch **keines davon live getestet**. **Laufendes Nutzer-Feedback (2026-07-26):** Nutzer geht gerade eine Liste an Dingen durch, die beim Durchdenken/Testen nicht gepasst haben, und nennt sie einzeln nacheinander in Folge-Prompts — erster Punkt bereits behoben: `ClientMenuScreen` scrollte nicht (siehe 5e-Nachbesserung oben). Diese Liste hat Vorrang vor dem normalen Live-Test-Durchgang unten, bis sie abgearbeitet ist. Offene Punkte, unabhängig voneinander:
1. **5g–5o und jetzt 5q live testen** (`runClient` reicht, wie bei 5a–5f) — am besten in dieser Reihenfolge: zuerst 5j (Fullbright) und 5o (Itemphysics, gerade fertig geworden), die beiden riskantesten Mixins (Accessor bzw. komplette Methodenersetzung), dann 5l/5q (Hitbox-/Blockumriss-Farbe, Methodenersetzung im selben Muster), danach den Rest (5g/5h/5i/5k/5m/5n sind alle additiv bzw. mixin-frei, geringeres Risiko). Dabei auch die neue Keystrokes-Tasten-Ein/Aus-Funktion (`KeystrokesKeysOptionsScreen`) mittesten - rein additiv, kein Mixin, aber noch nicht live gesehen. **5r (Dunkles Inventar) ist bereits fertig getestet und bestätigt**, hier nichts mehr offen. Dabei auch entscheiden, ob die Launcher-Instanz jetzt auch aktualisiert werden soll (neuer Mod-Jar + fehlendes Continuity nachziehen) oder ob `runClient` weiterhin der primäre Testweg bleibt.
2. **Phase 3 abschließen:** weiter warten auf Antwort zum (erneut eingereichten, 2026-07-18) Minecraft-API-Freischaltungsantrag (`aka.ms/mce-reviewappid`). Sobald sie da ist: `completeMinecraftLogin` in `launcher/src/main/auth/minecraftAuth.ts` real ohne Mock-Fallback testen (einfach nochmal "Mit Microsoft anmelden" klicken — kein "Dev-Mock-Profil"-Badge mehr sollte dann erscheinen), Phase 3 als ✅ markieren.
3. **Phase 6 angehen** (Launcher-UX: Versionsauswahl & Mod-Verwaltung, siehe Roadmap) — Versions-Picker, persistente Instanz-Konfiguration, Mods-Screen (gebündelte Mods an/ausschalten + eigene Fabric-Mod-Jars hinzufügen). Sollte dabei auch gleich die manuelle resourcepacks-Kopie aus 5p automatisieren. Blockiert nicht durch das Phase-3-Warten.
4. **5d optional nachschärfen**, sobald der Nutzer konkreteres Feedback von der Person hat, die das F3-Feature ursprünglich vorgeschlagen hat.
5. **Phase 7+ / Lizenz-Punkte im Hinterkopf behalten:** durch 5f ist jetzt zum ersten Mal ein fremder Mod (Continuity, LGPL-3.0-only) gebündelt, durch 5p zum ersten Mal ein volles GPL-3.0-Asset (3D Default), durch 5r jetzt zum ersten Mal eine Creative-Commons-Lizenz (Default Dark Mode, CC-BY-NC-SA-4.0, siehe Lizenz-Hinweis oben) — der laut Roadmap ohnehin geplante "Third-Party Licenses/Credits"-Screen (Mod + Launcher) hat damit drei echte Einträge. Bei Default Dark Mode zusätzlich die NC-Klausel im Hinterkopf behalten, falls das Projekt jemals monetarisiert werden sollte. 5o (Itemphysics) wurde bewusst **nicht** über einen gebündelten Fremd-Mod gelöst (siehe 5o-Abschnitt oben, Server-Side-Problem), also kein weiterer Eintrag von dort.
6. **Nicht in Phase 5 aufgenommen, für später/Ende vorgemerkt:** Farbschema-Auswahl, Willkommens-/Setup-Wizard beim ersten Start, Discord Rich Presence, Cross-Platform-Kompatibilität, Auto-Update — das sind Launcher-/Cross-Cutting-Themen, keine Mod-Features (siehe Roadmap-Phasen 6/8/9). Proximity Voice Chat ist bewusst außen vor: zu groß für "leicht bis schwer" (eigenes Backend/Audio-Pipeline nötig, siehe Phase 8) — Kandidat für eine eigene spätere Phase oder Bündelung eines bestehenden Mods (z.B. Simple Voice Chat) nach dem 5f-Muster.
